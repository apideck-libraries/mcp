/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod";
export const CustomFieldValue3$zodSchema = z
    .union([
    z.string(),
    z.number(),
    z.boolean(),
    z.record(z.string(), z.any()),
]);
export const CustomFieldValue4$zodSchema = z
    .union([
    z.string(),
    z.number(),
    z.boolean(),
    z.record(z.string(), z.any()),
    z.array(z.union([
        z.string(),
        z.number(),
        z.boolean(),
        z.record(z.string(), z.any()),
    ]).nullable()),
]);
export const CustomField2$zodSchema = z.object({
    description: z.string().nullable().optional().describe("More information about the custom field"),
    id: z.string().nullable().optional().describe("Unique identifier for the custom field."),
    name: z.string().nullable().describe("Name of the custom field."),
    value: z.union([
        z.string(),
        z.number(),
        z.boolean(),
        z.record(z.string(), z.any()),
        z.array(z.union([
            z.string(),
            z.number(),
            z.boolean(),
            z.record(z.string(), z.any()),
        ]).nullable()),
    ]).nullable().optional(),
});
export const CustomFieldValue1$zodSchema = z
    .union([
    z.string(),
    z.number(),
    z.boolean(),
    z.record(z.string(), z.any()),
]);
export const CustomFieldValue2$zodSchema = z
    .union([
    z.string(),
    z.number(),
    z.boolean(),
    z.record(z.string(), z.any()),
    z.array(z.union([
        z.string(),
        z.number(),
        z.boolean(),
        z.record(z.string(), z.any()),
    ]).nullable()),
]);
export const CustomField1$zodSchema = z.object({
    description: z.string().nullable().optional().describe("More information about the custom field"),
    id: z.string().nullable().describe("Unique identifier for the custom field."),
    name: z.string().nullable().optional().describe("Name of the custom field."),
    value: z.union([
        z.string(),
        z.number(),
        z.boolean(),
        z.record(z.string(), z.any()),
        z.array(z.union([
            z.string(),
            z.number(),
            z.boolean(),
            z.record(z.string(), z.any()),
        ]).nullable()),
    ]).nullable().optional(),
});
export const CustomFieldUnion$zodSchema = z.union([
    z.lazy(() => CustomField1$zodSchema),
    z.lazy(() => CustomField2$zodSchema),
]);
//# sourceMappingURL=customfieldunion.js.map