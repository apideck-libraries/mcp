/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod";
import { catchUnrecognizedEnum } from "../types/enums.js";
/**
 * Mode of the webhook support.
 */
export const WebhookSupportMode = {
    Native: "native",
    Virtual: "virtual",
    None: "none",
};
export const WebhookSupportMode$zodSchema = z.union([
    z.enum([
        "native",
        "virtual",
        "none",
    ]),
    z.string().transform(catchUnrecognizedEnum),
]).describe("Mode of the webhook support.");
/**
 * Received events are scoped to connection or across integration.
 */
export const SubscriptionLevel = {
    Connection: "connection",
    Integration: "integration",
};
export const SubscriptionLevel$zodSchema = z.union([
    z.enum([
        "connection",
        "integration",
    ]),
    z.string().transform(catchUnrecognizedEnum),
]).describe("Received events are scoped to connection or across integration.");
/**
 * How the subscription is managed in the downstream.
 */
export const ManagedVia = {
    Manual: "manual",
    Api: "api",
};
export const ManagedVia$zodSchema = z.union([
    z.enum([
        "manual",
        "api",
    ]),
    z.string().transform(catchUnrecognizedEnum),
]).describe("How the subscription is managed in the downstream.");
/**
 * The window unit for the rate.
 */
export const Unit = {
    Second: "second",
    Minute: "minute",
    Hour: "hour",
    Day: "day",
};
export const Unit$zodSchema = z.union([
    z.enum([
        "second",
        "minute",
        "hour",
        "day",
    ]),
    z.string().transform(catchUnrecognizedEnum),
]).describe("The window unit for the rate.");
export const RequestRate$zodSchema = z.object({
    rate: z.int().describe("The number of requests per window unit."),
    size: z.int().describe("Size of request window."),
    unit: Unit$zodSchema.describe("The window unit for the rate."),
}).describe("The rate at which requests for resources will be made to downstream.");
export const WebhookSupportResources$zodSchema = z.object({
    events: z.array(z.string()).optional(),
});
export const VirtualWebhooks$zodSchema = z.object({
    request_rate: z.lazy(() => RequestRate$zodSchema).describe("The rate at which requests for resources will be made to downstream."),
    resources: z.record(z.string(), z.lazy(() => WebhookSupportResources$zodSchema)).optional().describe("The resources that will be requested from downstream."),
}).describe("Virtual webhook config for the connector.");
export const WebhookSupport$zodSchema = z.object({
    managed_via: ManagedVia$zodSchema.optional().describe("How the subscription is managed in the downstream."),
    mode: WebhookSupportMode$zodSchema.optional().describe("Mode of the webhook support."),
    subscription_level: SubscriptionLevel$zodSchema.optional().describe("Received events are scoped to connection or across integration."),
    virtual_webhooks: z.lazy(() => VirtualWebhooks$zodSchema).optional().describe("Virtual webhook config for the connector."),
}).describe("How webhooks are supported for the connector. Sometimes the connector natively supports webhooks, other times Apideck virtualizes them based on polling.");
//# sourceMappingURL=webhooksupport.js.map