/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import * as z from "zod";
import * as b64$ from "../lib/base64.js";
import { Unauthorized$zodSchema } from "./unauthorized.js";
export const ProxyPutProxyGlobals$zodSchema = z
    .object({
    xApideckAppId: z.string().describe("The ID of your Unify application")
        .optional(),
    xApideckConsumerId: z.string().describe("ID of the consumer which you want to get or push data from").optional(),
});
export const ProxyPutProxyRequestBody2$zodSchema = z.object({});
export const ProxyPutProxyRequestBody1$zodSchema = z.object({});
export const ProxyPutProxyRequest$zodSchema = z
    .object({
    body: z.string().describe("Base64-encoded binary content").transform(b64$.bytesFromBase64).optional(),
    xApideckAppId: z.string().describe("The ID of your Unify application")
        .optional(),
    xApideckConsumerId: z.string().describe("ID of the consumer which you want to get or push data from").optional(),
    xApideckDownstreamAuthorization: z.string().describe("Downstream authorization header. This will skip the Vault token injection.").optional(),
    xApideckDownstreamUrl: z.string().describe("Downstream URL"),
    xApideckServiceId: z.string().describe("Provide the service id you want to call (e.g., pipedrive). Only needed when a consumer has activated multiple integrations for a Unified API."),
    xApideckUnifiedApi: z.string().describe("Specify which unified API to use for the connection lookup. Required for multi-API connectors (e.g., Workday) to ensure the correct credentials are used.").optional(),
});
export const ProxyPutProxyResponseResult$zodSchema = z.union([
    z.record(z.string(), z.any()).describe("Ok"),
    z.string().describe("Base64-encoded binary content").transform(b64$.bytesFromBase64),
    z.string().describe("Base64-encoded binary content").transform(b64$.bytesFromBase64),
    z.string().describe("Ok"),
    z.string().describe("Ok"),
    z.string().describe("Ok"),
    Unauthorized$zodSchema,
    z.record(z.string(), z.any()).describe("Proxy error"),
    z.string().describe("Proxy error"),
    z.string().describe("Proxy error"),
    z.string().describe("Proxy error"),
]);
export const ProxyPutProxyResponse$zodSchema = z.object({
    Headers: z.record(z.string(), z.array(z.string())).default({}),
    Result: z.union([
        z.record(z.string(), z.any()).describe("Ok"),
        z.string().describe("Base64-encoded binary content").transform(b64$.bytesFromBase64),
        z.string().describe("Base64-encoded binary content").transform(b64$.bytesFromBase64),
        z.string().describe("Ok"),
        z.string().describe("Ok"),
        z.string().describe("Ok"),
        Unauthorized$zodSchema,
        z.record(z.string(), z.any()).describe("Proxy error"),
        z.string().describe("Proxy error"),
        z.string().describe("Proxy error"),
        z.string().describe("Proxy error"),
    ]),
});
//# sourceMappingURL=proxyputproxyop.js.map