/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { fileStorageUploadSessionsUpload } from "../../funcs/fileStorageUploadSessionsUpload.js";
import { FileStorageUploadSessionsUploadRequest$zodSchema } from "../../models/filestorageuploadsessionsuploadop.js";
import { formatResult } from "../tools.js";
const args = {
    request: FileStorageUploadSessionsUploadRequest$zodSchema,
};
export const tool$fileStorageUploadSessionsUpload = {
    name: "file-storage-upload-sessions-upload",
    description: `Upload part of File to Upload Session

Upload part of File to Upload Session (max 100MB). Get \`part_size\` from [Get Upload Session](#operation/uploadSessionsOne) first. Every File part (except the last one) uploaded to this endpoint should have Content-Length equal to \`part_size\`. Note that the base URL is upload.apideck.com instead of unify.apideck.com. For more information on uploads, refer to the [file upload guide](/guides/file-upload).`,
    scopes: ["write"],
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await fileStorageUploadSessionsUpload(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=fileStorageUploadSessionsUpload.js.map