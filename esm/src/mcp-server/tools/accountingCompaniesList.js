/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { accountingCompaniesList } from "../../funcs/accountingCompaniesList.js";
import { AccountingCompaniesAllRequest$zodSchema } from "../../models/accountingcompaniesallop.js";
import { formatResult } from "../tools.js";
const args = {
    request: AccountingCompaniesAllRequest$zodSchema.optional(),
};
export const tool$accountingCompaniesList = {
    name: "accounting-companies-list",
    description: `List companies

List all companies accessible through the current connection. Only connectors that support multi-company access (x-apideck-supports-multi-company) will return a list of companies. Use the returned company ID in the x-apideck-company-id header to scope subsequent requests to a specific company.`,
    scopes: ["read"],
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await accountingCompaniesList(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=accountingCompaniesList.js.map