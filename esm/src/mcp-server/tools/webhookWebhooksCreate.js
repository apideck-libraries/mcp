/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { webhookWebhooksCreate } from "../../funcs/webhookWebhooksCreate.js";
import { WebhookWebhooksAddRequest$zodSchema } from "../../models/webhookwebhooksaddop.js";
import { formatResult } from "../tools.js";
const args = {
    request: WebhookWebhooksAddRequest$zodSchema,
};
export const tool$webhookWebhooksCreate = {
    name: "webhook-webhooks-create",
    description: `Create webhook subscription

Create a webhook subscription to receive events.

**Delivery URL Validation**: The provided \`delivery_url\` will be validated synchronously by sending an HTTP POST request with an HMAC signature. If validation fails (network error, timeout, non-2xx response), the webhook will still be created but with \`status: "disabled"\` and \`disabled_reason: "delivery_url_validation_failed"\`.

**Important**: Always check the \`status\` and \`disabled_reason\` fields in the response to ensure the webhook is active.
`,
    scopes: ["write"],
    annotations: {
        "title": "",
        "destructiveHint": false,
        "idempotentHint": false,
        "openWorldHint": false,
        "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
        const [result] = await webhookWebhooksCreate(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        return formatResult(result.value);
    },
};
//# sourceMappingURL=webhookWebhooksCreate.js.map