/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  PaginationCoverage,
  PaginationCoverage$zodSchema,
} from "./paginationcoverage.js";
import { ResourceStatus, ResourceStatus$zodSchema } from "./resourcestatus.js";
import {
  SupportedProperty,
  SupportedProperty$zodSchema,
} from "./supportedproperty.js";

export type Coverage = {
  downstream_id?: string | undefined;
  downstream_name?: string | undefined;
  pagination_supported?: boolean | undefined;
  pagination?: PaginationCoverage | undefined;
  supported_operations?: Array<string> | undefined;
  supported_filters?: Array<string> | undefined;
  supported_sort_by?: Array<string> | undefined;
  supported_fields?: Array<SupportedProperty> | undefined;
  supported_list_fields?: Array<SupportedProperty> | undefined;
};

export const Coverage$zodSchema: z.ZodType<Coverage> = z.object({
  downstream_id: z.string().optional().describe(
    "ID of the resource in the Connector's API (downstream)",
  ),
  downstream_name: z.string().optional().describe(
    "Name of the resource in the Connector's API (downstream)",
  ),
  pagination: PaginationCoverage$zodSchema.optional(),
  pagination_supported: z.boolean().optional().describe(
    "Indicates if pagination (cursor and limit parameters) is supported on the list endpoint of the resource.",
  ),
  supported_fields: z.array(SupportedProperty$zodSchema).optional().describe(
    "Supported fields on the detail endpoint.",
  ),
  supported_filters: z.array(z.string()).optional().describe(
    "Supported filters on the list endpoint of the resource.",
  ),
  supported_list_fields: z.array(SupportedProperty$zodSchema).optional()
    .describe("Supported fields on the list endpoint."),
  supported_operations: z.array(z.string()).optional().describe(
    "List of supported operations on the resource.",
  ),
  supported_sort_by: z.array(z.string()).optional().describe(
    "Supported sorting properties on the list endpoint of the resource.",
  ),
});

export type ApiResourceCoverage = {
  id?: string | undefined;
  name?: string | undefined;
  status?: ResourceStatus | undefined;
  coverage?: Array<Coverage> | undefined;
};

export const ApiResourceCoverage$zodSchema: z.ZodType<ApiResourceCoverage> = z
  .object({
    coverage: z.array(z.lazy(() => Coverage$zodSchema)).optional(),
    id: z.string().optional().describe(
      "ID of the resource, typically a lowercased version of name.",
    ),
    name: z.string().optional().describe("Name of the resource (plural)"),
    status: ResourceStatus$zodSchema.optional().describe(
      "Status of the resource. Resources with status live or beta are callable.",
    ),
  });
