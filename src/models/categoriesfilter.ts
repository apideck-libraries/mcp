/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * The type of the category.
 */
export const CategoriesFilterType = {
  Supplier: "supplier",
  Expense: "expense",
  Revenue: "revenue",
  Customer: "customer",
} as const;
/**
 * The type of the category.
 */
export type CategoriesFilterType = OpenEnum<typeof CategoriesFilterType>;

export const CategoriesFilterType$zodSchema = z.union([
  z.enum([
    "supplier",
    "expense",
    "revenue",
    "customer",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The type of the category.");

export type CategoriesFilter = { type?: CategoriesFilterType | undefined };

export const CategoriesFilter$zodSchema: z.ZodType<CategoriesFilter> = z.object(
  {
    type: CategoriesFilterType$zodSchema.optional().describe(
      "The type of the category.",
    ),
  },
);
