/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * Type of the transaction.
 */
export const BalanceByTransactionTransactionType = {
  Invoice: "invoice",
  CreditNote: "credit_note",
  Bill: "bill",
  Payment: "payment",
  BillPayment: "bill_payment",
} as const;
/**
 * Type of the transaction.
 */
export type BalanceByTransactionTransactionType = OpenEnum<
  typeof BalanceByTransactionTransactionType
>;

export const BalanceByTransactionTransactionType$zodSchema = z.union([
  z.enum([
    "invoice",
    "credit_note",
    "bill",
    "payment",
    "bill_payment",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Type of the transaction.");

export type BalanceByTransaction = {
  transaction_id?: string | undefined;
  transaction_date?: string | undefined;
  transaction_type?: BalanceByTransactionTransactionType | undefined;
  due_date?: string | undefined;
  original_amount?: number | undefined;
  outstanding_balance?: number | undefined;
  transaction_number?: string | undefined;
};

export const BalanceByTransaction$zodSchema: z.ZodType<BalanceByTransaction> = z
  .object({
    due_date: z.string().date().optional().describe(
      "Due date of the transaction.",
    ),
    original_amount: z.number().optional().describe(
      "Original amount of the transaction.",
    ),
    outstanding_balance: z.number().optional().describe(
      "Outstanding balance of the transaction.",
    ),
    transaction_date: z.string().date().optional().describe(
      "Date of the transaction.",
    ),
    transaction_id: z.string().optional().describe(
      "Unique identifier for the transaction.",
    ),
    transaction_number: z.string().optional().describe(
      "Transaction number of the transaction.",
    ),
    transaction_type: BalanceByTransactionTransactionType$zodSchema.optional()
      .describe("Type of the transaction."),
  });
