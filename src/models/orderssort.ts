/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Orders
 */
export const OrdersSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
  Name: "name",
} as const;
/**
 * The field on which to sort the Orders
 */
export type OrdersSortBy = OpenEnum<typeof OrdersSortBy>;

export const OrdersSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
    "name",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Orders");

export type OrdersSort = {
  by?: OrdersSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const OrdersSort$zodSchema: z.ZodType<OrdersSort> = z.object({
  by: OrdersSortBy$zodSchema.optional().describe(
    "The field on which to sort the Orders",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
