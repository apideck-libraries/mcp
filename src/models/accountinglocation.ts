/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { Address, Address$zodSchema } from "./address.js";
import {
  PassThroughBody,
  PassThroughBody$zodSchema,
} from "./passthroughbody.js";
import {
  SubsidiaryReference,
  SubsidiaryReference$zodSchema,
} from "./subsidiaryreference.js";
import {
  SubsidiaryReferenceInput,
  SubsidiaryReferenceInput$zodSchema,
} from "./subsidiaryreferenceinput.js";

/**
 * Based on the status some functionality is enabled or disabled.
 */
export const LocationStatus = {
  Active: "active",
  Inactive: "inactive",
} as const;
/**
 * Based on the status some functionality is enabled or disabled.
 */
export type LocationStatus = OpenEnum<typeof LocationStatus>;

export const LocationStatus$zodSchema = z.union([
  z.enum([
    "active",
    "inactive",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Based on the status some functionality is enabled or disabled.");

export type AccountingLocation = {
  id?: string | undefined;
  parent_id?: string | null | undefined;
  display_id?: string | null | undefined;
  downstream_id?: string | null | undefined;
  company_name?: string | null | undefined;
  display_name?: string | null | undefined;
  status?: LocationStatus | undefined;
  addresses?: Array<Address> | undefined;
  subsidiaries?: Array<SubsidiaryReference> | undefined;
  custom_mappings?: { [k: string]: any } | null | undefined;
  row_version?: string | null | undefined;
  updated_by?: string | null | undefined;
  created_by?: string | null | undefined;
  updated_at?: string | null | undefined;
  created_at?: string | null | undefined;
  pass_through?: Array<PassThroughBody> | undefined;
};

export const AccountingLocation$zodSchema: z.ZodType<AccountingLocation> = z
  .object({
    addresses: z.array(Address$zodSchema).optional(),
    company_name: z.string().nullable().optional().describe(
      "The name of the company.",
    ),
    created_at: z.iso.datetime({ offset: true }).nullable().optional().describe(
      "The date and time when the object was created.",
    ),
    created_by: z.string().nullable().optional().describe(
      "The user who created the object.",
    ),
    custom_mappings: z.record(z.string(), z.any()).nullable().optional()
      .describe(
        "When custom mappings are configured on the resource, the result is included here.",
      ),
    display_id: z.string().nullable().optional().describe(
      "Id to be displayed.",
    ),
    display_name: z.string().nullable().optional().describe(
      "The display name of the location.",
    ),
    downstream_id: z.string().nullable().optional().describe(
      "The third-party API ID of original entity",
    ),
    id: z.string().optional().describe("A unique identifier for an object."),
    parent_id: z.string().nullable().optional().describe(
      "A unique identifier for an object.",
    ),
    pass_through: z.array(PassThroughBody$zodSchema).optional().describe(
      "The pass_through property allows passing service-specific, custom data or structured modifications in request body when creating or updating resources.",
    ),
    row_version: z.string().nullable().optional().describe(
      "A binary value used to detect updates to a object and prevent data conflicts. It is incremented each time an update is made to the object.",
    ),
    status: LocationStatus$zodSchema.optional().describe(
      "Based on the status some functionality is enabled or disabled.",
    ),
    subsidiaries: z.array(SubsidiaryReference$zodSchema).optional(),
    updated_at: z.iso.datetime({ offset: true }).nullable().optional().describe(
      "The date and time when the object was last updated.",
    ),
    updated_by: z.string().nullable().optional().describe(
      "The user who last updated the object.",
    ),
  });

export type AccountingLocationInput = {
  parent_id?: string | null | undefined;
  display_id?: string | null | undefined;
  company_name?: string | null | undefined;
  display_name?: string | null | undefined;
  status?: LocationStatus | undefined;
  addresses?: Array<Address> | undefined;
  subsidiaries?: Array<SubsidiaryReferenceInput> | undefined;
  row_version?: string | null | undefined;
  pass_through?: Array<PassThroughBody> | undefined;
};

export const AccountingLocationInput$zodSchema: z.ZodType<
  AccountingLocationInput
> = z.object({
  addresses: z.array(Address$zodSchema).optional(),
  company_name: z.string().nullable().optional().describe(
    "The name of the company.",
  ),
  display_id: z.string().nullable().optional().describe("Id to be displayed."),
  display_name: z.string().nullable().optional().describe(
    "The display name of the location.",
  ),
  parent_id: z.string().nullable().optional().describe(
    "A unique identifier for an object.",
  ),
  pass_through: z.array(PassThroughBody$zodSchema).optional().describe(
    "The pass_through property allows passing service-specific, custom data or structured modifications in request body when creating or updating resources.",
  ),
  row_version: z.string().nullable().optional().describe(
    "A binary value used to detect updates to a object and prevent data conflicts. It is incremented each time an update is made to the object.",
  ),
  status: LocationStatus$zodSchema.optional().describe(
    "Based on the status some functionality is enabled or disabled.",
  ),
  subsidiaries: z.array(SubsidiaryReferenceInput$zodSchema).optional(),
});
