/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import {
  PassThroughBody,
  PassThroughBody$zodSchema,
} from "./passthroughbody.js";

export type CreateUploadSessionRequest = {
  name: string;
  parent_folder_id: string;
  drive_id?: string | undefined;
  size: number | null;
  pass_through?: Array<PassThroughBody> | undefined;
};

export const CreateUploadSessionRequest$zodSchema: z.ZodType<
  CreateUploadSessionRequest
> = z.object({
  drive_id: z.string().optional().describe("ID of the drive to upload to."),
  name: z.string().describe("The name of the file."),
  parent_folder_id: z.string().describe(
    "The parent folder to create the new file within. This can be an ID or a path depending on the downstream folder. Please see the connector section below to see downstream specific gotchas.",
  ),
  pass_through: z.array(PassThroughBody$zodSchema).optional().describe(
    "The pass_through property allows passing service-specific, custom data or structured modifications in request body when creating or updating resources.",
  ),
  size: z.int().nullable().describe("The size of the file in bytes"),
});
