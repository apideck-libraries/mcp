/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type FilesFilter = {
  drive_id?: string | undefined;
  folder_id?: string | undefined;
  shared?: boolean | undefined;
};

export const FilesFilter$zodSchema: z.ZodType<FilesFilter> = z.object({
  drive_id: z.string().optional().describe("ID of the drive to filter on"),
  folder_id: z.string().optional().describe(
    "ID of the folder to filter on. The root folder has an alias \"root\"",
  ),
  shared: z.boolean().optional().describe(
    "Only return files and folders that are shared",
  ),
});
