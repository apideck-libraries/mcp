/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Employees
 */
export const EmployeesSortBy = {
  FirstName: "first_name",
  LastName: "last_name",
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
} as const;
/**
 * The field on which to sort the Employees
 */
export type EmployeesSortBy = OpenEnum<typeof EmployeesSortBy>;

export const EmployeesSortBy$zodSchema = z.union([
  z.enum([
    "first_name",
    "last_name",
    "created_at",
    "updated_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Employees");

export type EmployeesSort = {
  by?: EmployeesSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const EmployeesSort$zodSchema: z.ZodType<EmployeesSort> = z.object({
  by: EmployeesSortBy$zodSchema.optional().describe(
    "The field on which to sort the Employees",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
