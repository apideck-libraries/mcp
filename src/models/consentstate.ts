/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * The current consent state of the connection
 */
export const ConsentState = {
  Implicit: "implicit",
  Pending: "pending",
  Granted: "granted",
  Denied: "denied",
  Revoked: "revoked",
  RequiresReconsent: "requires_reconsent",
} as const;
/**
 * The current consent state of the connection
 */
export type ConsentState = OpenEnum<typeof ConsentState>;

export const ConsentState$zodSchema = z.union([
  z.enum([
    "implicit",
    "pending",
    "granted",
    "denied",
    "revoked",
    "requires_reconsent",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The current consent state of the connection");
