/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Companies
 */
export const CompaniesSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
  Name: "name",
} as const;
/**
 * The field on which to sort the Companies
 */
export type CompaniesSortBy = OpenEnum<typeof CompaniesSortBy>;

export const CompaniesSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
    "name",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Companies");

export type CompaniesSort = {
  by?: CompaniesSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const CompaniesSort$zodSchema: z.ZodType<CompaniesSort> = z.object({
  by: CompaniesSortBy$zodSchema.optional().describe(
    "The field on which to sort the Companies",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
