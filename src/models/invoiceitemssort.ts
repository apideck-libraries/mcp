/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Invoice Items
 */
export const InvoiceItemsSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
} as const;
/**
 * The field on which to sort the Invoice Items
 */
export type InvoiceItemsSortBy = OpenEnum<typeof InvoiceItemsSortBy>;

export const InvoiceItemsSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Invoice Items");

export type InvoiceItemsSort = {
  by?: InvoiceItemsSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const InvoiceItemsSort$zodSchema: z.ZodType<InvoiceItemsSort> = z.object(
  {
    by: InvoiceItemsSortBy$zodSchema.optional().describe(
      "The field on which to sort the Invoice Items",
    ),
    direction: SortDirection$zodSchema.default("asc").describe(
      "The direction in which to sort the results",
    ),
  },
);
