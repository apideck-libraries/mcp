/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Refunds
 */
export const RefundsSortBy = {
  UpdatedAt: "updated_at",
  CreatedAt: "created_at",
} as const;
/**
 * The field on which to sort the Refunds
 */
export type RefundsSortBy = OpenEnum<typeof RefundsSortBy>;

export const RefundsSortBy$zodSchema = z.union([
  z.enum([
    "updated_at",
    "created_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Refunds");

export type RefundsSort = {
  by?: RefundsSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const RefundsSort$zodSchema: z.ZodType<RefundsSort> = z.object({
  by: RefundsSortBy$zodSchema.optional().describe(
    "The field on which to sort the Refunds",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
