/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

export const ExpensesFilterStatus = {
  Draft: "draft",
  Posted: "posted",
  Voided: "voided",
} as const;
export type ExpensesFilterStatus = OpenEnum<typeof ExpensesFilterStatus>;

export const ExpensesFilterStatus$zodSchema = z.union([
  z.enum([
    "draft",
    "posted",
    "voided",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]);

export const ExpensesFilterType = {
  Expense: "expense",
  Refund: "refund",
} as const;
export type ExpensesFilterType = OpenEnum<typeof ExpensesFilterType>;

export const ExpensesFilterType$zodSchema = z.union([
  z.enum([
    "expense",
    "refund",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]);

export type ExpensesFilter = {
  updated_since?: string | undefined;
  status?: ExpensesFilterStatus | undefined;
  type?: ExpensesFilterType | undefined;
};

export const ExpensesFilter$zodSchema: z.ZodType<ExpensesFilter> = z.object({
  status: ExpensesFilterStatus$zodSchema.optional(),
  type: ExpensesFilterType$zodSchema.optional(),
  updated_since: z.iso.datetime({ offset: true }).optional(),
});
