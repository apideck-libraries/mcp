/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Comments
 */
export const CommentsSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
} as const;
/**
 * The field on which to sort the Comments
 */
export type CommentsSortBy = OpenEnum<typeof CommentsSortBy>;

export const CommentsSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Comments");

export type CommentsSort = {
  by?: CommentsSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const CommentsSort$zodSchema: z.ZodType<CommentsSort> = z.object({
  by: CommentsSortBy$zodSchema.optional().describe(
    "The field on which to sort the Comments",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
