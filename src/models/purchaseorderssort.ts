/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Purchase Orders
 */
export const PurchaseOrdersSortBy = {
  UpdatedAt: "updated_at",
  CreatedAt: "created_at",
} as const;
/**
 * The field on which to sort the Purchase Orders
 */
export type PurchaseOrdersSortBy = OpenEnum<typeof PurchaseOrdersSortBy>;

export const PurchaseOrdersSortBy$zodSchema = z.union([
  z.enum([
    "updated_at",
    "created_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Purchase Orders");

export type PurchaseOrdersSort = {
  by?: PurchaseOrdersSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const PurchaseOrdersSort$zodSchema: z.ZodType<PurchaseOrdersSort> = z
  .object({
    by: PurchaseOrdersSortBy$zodSchema.optional().describe(
      "The field on which to sort the Purchase Orders",
    ),
    direction: SortDirection$zodSchema.default("asc").describe(
      "The direction in which to sort the results",
    ),
  });
