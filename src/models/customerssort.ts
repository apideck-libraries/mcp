/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Customers
 */
export const CustomersSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
} as const;
/**
 * The field on which to sort the Customers
 */
export type CustomersSortBy = OpenEnum<typeof CustomersSortBy>;

export const CustomersSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Customers");

export type CustomersSort = {
  by?: CustomersSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const CustomersSort$zodSchema: z.ZodType<CustomersSort> = z.object({
  by: CustomersSortBy$zodSchema.optional().describe(
    "The field on which to sort the Customers",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
