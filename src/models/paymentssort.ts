/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Payments
 */
export const PaymentsSortBy = {
  UpdatedAt: "updated_at",
  CreatedAt: "created_at",
} as const;
/**
 * The field on which to sort the Payments
 */
export type PaymentsSortBy = OpenEnum<typeof PaymentsSortBy>;

export const PaymentsSortBy$zodSchema = z.union([
  z.enum([
    "updated_at",
    "created_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Payments");

export type PaymentsSort = {
  by?: PaymentsSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const PaymentsSort$zodSchema: z.ZodType<PaymentsSort> = z.object({
  by: PaymentsSortBy$zodSchema.optional().describe(
    "The field on which to sort the Payments",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
