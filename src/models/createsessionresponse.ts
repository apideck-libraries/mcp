/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type CreateSessionResponseData = {
  session_uri: string;
  session_token: string;
};

export const CreateSessionResponseData$zodSchema: z.ZodType<
  CreateSessionResponseData
> = z.object({
  session_token: z.string(),
  session_uri: z.string(),
});

/**
 * Session created
 */
export type CreateSessionResponse = {
  status_code: number;
  status: string;
  data: CreateSessionResponseData;
  _raw?: { [k: string]: any } | null | undefined;
};

export const CreateSessionResponse$zodSchema: z.ZodType<CreateSessionResponse> =
  z.object({
    _raw: z.record(z.string(), z.any()).nullable().optional().describe(
      "Raw response from the integration when raw=true query param is provided",
    ),
    data: z.lazy(() => CreateSessionResponseData$zodSchema),
    status: z.string().describe("HTTP Response Status"),
    status_code: z.int().describe("HTTP Response Status Code"),
  }).describe("Session created");
