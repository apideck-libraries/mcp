/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Leads
 */
export const LeadsSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
  Name: "name",
  FirstName: "first_name",
  LastName: "last_name",
  Email: "email",
} as const;
/**
 * The field on which to sort the Leads
 */
export type LeadsSortBy = OpenEnum<typeof LeadsSortBy>;

export const LeadsSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
    "name",
    "first_name",
    "last_name",
    "email",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Leads");

export type LeadsSort = {
  by?: LeadsSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const LeadsSort$zodSchema: z.ZodType<LeadsSort> = z.object({
  by: LeadsSortBy$zodSchema.optional().describe(
    "The field on which to sort the Leads",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
