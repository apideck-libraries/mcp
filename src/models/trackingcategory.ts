/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import {
  PassThroughBody,
  PassThroughBody$zodSchema,
} from "./passthroughbody.js";

/**
 * Based on the status some functionality is enabled or disabled.
 */
export const TrackingCategoryStatus = {
  Active: "active",
  Inactive: "inactive",
} as const;
/**
 * Based on the status some functionality is enabled or disabled.
 */
export type TrackingCategoryStatus = OpenEnum<typeof TrackingCategoryStatus>;

export const TrackingCategoryStatus$zodSchema = z.union([
  z.enum([
    "active",
    "inactive",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Based on the status some functionality is enabled or disabled.");

export type TrackingCategorySubsidiary = { id?: string | undefined };

export const TrackingCategorySubsidiary$zodSchema: z.ZodType<
  TrackingCategorySubsidiary
> = z.object({
  id: z.string().optional().describe("The ID of the subsidiary."),
});

export type TrackingCategory = {
  id?: string | undefined;
  parent_id?: string | null | undefined;
  parent_name?: string | null | undefined;
  name?: string | undefined;
  code?: string | null | undefined;
  status?: TrackingCategoryStatus | undefined;
  custom_mappings?: { [k: string]: any } | null | undefined;
  row_version?: string | null | undefined;
  updated_by?: string | null | undefined;
  created_by?: string | null | undefined;
  updated_at?: string | null | undefined;
  created_at?: string | null | undefined;
  pass_through?: Array<PassThroughBody> | undefined;
  subsidiaries?: Array<TrackingCategorySubsidiary> | undefined;
};

export const TrackingCategory$zodSchema: z.ZodType<TrackingCategory> = z.object(
  {
    code: z.string().nullable().optional().describe(
      "The code of the tracking category.",
    ),
    created_at: z.iso.datetime({ offset: true }).nullable().optional().describe(
      "The date and time when the object was created.",
    ),
    created_by: z.string().nullable().optional().describe(
      "The user who created the object.",
    ),
    custom_mappings: z.record(z.string(), z.any()).nullable().optional()
      .describe(
        "When custom mappings are configured on the resource, the result is included here.",
      ),
    id: z.string().optional().describe("A unique identifier for an object."),
    name: z.string().optional().describe("The name of the tracking category."),
    parent_id: z.string().nullable().optional().describe(
      "A unique identifier for an object.",
    ),
    parent_name: z.string().nullable().optional().describe(
      "The name of the parent tracking category.",
    ),
    pass_through: z.array(PassThroughBody$zodSchema).optional().describe(
      "The pass_through property allows passing service-specific, custom data or structured modifications in request body when creating or updating resources.",
    ),
    row_version: z.string().nullable().optional().describe(
      "A binary value used to detect updates to a object and prevent data conflicts. It is incremented each time an update is made to the object.",
    ),
    status: TrackingCategoryStatus$zodSchema.optional().describe(
      "Based on the status some functionality is enabled or disabled.",
    ),
    subsidiaries: z.array(z.lazy(() => TrackingCategorySubsidiary$zodSchema))
      .optional().describe("The subsidiaries the account belongs to."),
    updated_at: z.iso.datetime({ offset: true }).nullable().optional().describe(
      "The date and time when the object was last updated.",
    ),
    updated_by: z.string().nullable().optional().describe(
      "The user who last updated the object.",
    ),
  },
);

export type TrackingCategoryInput = {
  parent_id?: string | null | undefined;
  parent_name?: string | null | undefined;
  name?: string | undefined;
  code?: string | null | undefined;
  status?: TrackingCategoryStatus | undefined;
  row_version?: string | null | undefined;
  pass_through?: Array<PassThroughBody> | undefined;
  subsidiaries?: Array<TrackingCategorySubsidiary> | undefined;
};

export const TrackingCategoryInput$zodSchema: z.ZodType<TrackingCategoryInput> =
  z.object({
    code: z.string().nullable().optional().describe(
      "The code of the tracking category.",
    ),
    name: z.string().optional().describe("The name of the tracking category."),
    parent_id: z.string().nullable().optional().describe(
      "A unique identifier for an object.",
    ),
    parent_name: z.string().nullable().optional().describe(
      "The name of the parent tracking category.",
    ),
    pass_through: z.array(PassThroughBody$zodSchema).optional().describe(
      "The pass_through property allows passing service-specific, custom data or structured modifications in request body when creating or updating resources.",
    ),
    row_version: z.string().nullable().optional().describe(
      "A binary value used to detect updates to a object and prevent data conflicts. It is incremented each time an update is made to the object.",
    ),
    status: TrackingCategoryStatus$zodSchema.optional().describe(
      "Based on the status some functionality is enabled or disabled.",
    ),
    subsidiaries: z.array(z.lazy(() => TrackingCategorySubsidiary$zodSchema))
      .optional().describe("The subsidiaries the account belongs to."),
  });
