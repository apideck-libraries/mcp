/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Journal Entries.
 */
export const JournalEntriesSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
} as const;
/**
 * The field on which to sort the Journal Entries.
 */
export type JournalEntriesSortBy = OpenEnum<typeof JournalEntriesSortBy>;

export const JournalEntriesSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Journal Entries.");

export type JournalEntriesSort = {
  by?: JournalEntriesSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const JournalEntriesSort$zodSchema: z.ZodType<JournalEntriesSort> = z
  .object({
    by: JournalEntriesSortBy$zodSchema.optional().describe(
      "The field on which to sort the Journal Entries.",
    ),
    direction: SortDirection$zodSchema.default("asc").describe(
      "The direction in which to sort the results",
    ),
  });
