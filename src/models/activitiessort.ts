/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";
import { SortDirection, SortDirection$zodSchema } from "./sortdirection.js";

/**
 * The field on which to sort the Activities
 */
export const ActivitiesSortBy = {
  CreatedAt: "created_at",
  UpdatedAt: "updated_at",
} as const;
/**
 * The field on which to sort the Activities
 */
export type ActivitiesSortBy = OpenEnum<typeof ActivitiesSortBy>;

export const ActivitiesSortBy$zodSchema = z.union([
  z.enum([
    "created_at",
    "updated_at",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("The field on which to sort the Activities");

export type ActivitiesSort = {
  by?: ActivitiesSortBy | undefined;
  direction?: SortDirection | undefined;
};

export const ActivitiesSort$zodSchema: z.ZodType<ActivitiesSort> = z.object({
  by: ActivitiesSortBy$zodSchema.optional().describe(
    "The field on which to sort the Activities",
  ),
  direction: SortDirection$zodSchema.default("asc").describe(
    "The direction in which to sort the results",
  ),
});
