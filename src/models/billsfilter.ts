/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * Filter by bill status
 */
export const BillsFilterStatus = {
  Paid: "paid",
  Unpaid: "unpaid",
  PartiallyPaid: "partially_paid",
} as const;
/**
 * Filter by bill status
 */
export type BillsFilterStatus = OpenEnum<typeof BillsFilterStatus>;

export const BillsFilterStatus$zodSchema = z.union([
  z.enum([
    "paid",
    "unpaid",
    "partially_paid",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Filter by bill status");

export type BillsFilter = {
  updated_since?: string | undefined;
  status?: BillsFilterStatus | undefined;
};

export const BillsFilter$zodSchema: z.ZodType<BillsFilter> = z.object({
  status: BillsFilterStatus$zodSchema.optional().describe(
    "Filter by bill status",
  ),
  updated_since: z.iso.datetime({ offset: true }).optional(),
});
