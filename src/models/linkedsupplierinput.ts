/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { Address, Address$zodSchema } from "./address.js";

/**
 * The supplier this entity is linked to.
 */
export type LinkedSupplierInput = {
  id?: string | undefined;
  display_name?: string | null | undefined;
  address?: Address | undefined;
};

export const LinkedSupplierInput$zodSchema: z.ZodType<LinkedSupplierInput> = z
  .object({
    address: Address$zodSchema.optional(),
    display_name: z.string().nullable().optional().describe(
      "The display name of the supplier.",
    ),
    id: z.string().optional().describe(
      "The ID of the supplier this entity is linked to.",
    ),
  }).describe("The supplier this entity is linked to.");
