/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * Filter by account type
 */
export const BankAccountsFilterAccountType = {
  Checking: "checking",
  Savings: "savings",
  CreditCard: "credit_card",
  MoneyMarket: "money_market",
  LineOfCredit: "line_of_credit",
  Other: "other",
  Cash: "cash",
} as const;
/**
 * Filter by account type
 */
export type BankAccountsFilterAccountType = OpenEnum<
  typeof BankAccountsFilterAccountType
>;

export const BankAccountsFilterAccountType$zodSchema = z.union([
  z.enum([
    "checking",
    "savings",
    "credit_card",
    "money_market",
    "line_of_credit",
    "other",
    "cash",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Filter by account type");

/**
 * Filter by account status
 */
export const BankAccountsFilterStatus = {
  Active: "active",
  Inactive: "inactive",
  Closed: "closed",
} as const;
/**
 * Filter by account status
 */
export type BankAccountsFilterStatus = OpenEnum<
  typeof BankAccountsFilterStatus
>;

export const BankAccountsFilterStatus$zodSchema = z.union([
  z.enum([
    "active",
    "inactive",
    "closed",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Filter by account status");

export type BankAccountsFilter = {
  name?: string | undefined;
  account_type?: BankAccountsFilterAccountType | undefined;
  status?: BankAccountsFilterStatus | undefined;
};

export const BankAccountsFilter$zodSchema: z.ZodType<BankAccountsFilter> = z
  .object({
    account_type: BankAccountsFilterAccountType$zodSchema.optional().describe(
      "Filter by account type",
    ),
    name: z.string().optional().describe("Filter by bank account name"),
    status: BankAccountsFilterStatus$zodSchema.optional().describe(
      "Filter by account status",
    ),
  });
