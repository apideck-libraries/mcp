/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

/**
 * An object representing a discount applied to an ecommerce order or product.
 */
export type EcommerceDiscount = {
  code?: string | null | undefined;
  amount?: string | null | undefined;
  percentage?: string | null | undefined;
};

export const EcommerceDiscount$zodSchema: z.ZodType<EcommerceDiscount> = z
  .object({
    amount: z.string().nullable().optional().describe(
      "The fixed amount of the discount.",
    ),
    code: z.string().nullable().optional().describe(
      "The code used to apply the discount.",
    ),
    percentage: z.string().nullable().optional().describe(
      "The percentage of the discount.",
    ),
  }).describe(
    "An object representing a discount applied to an ecommerce order or product.",
  );
