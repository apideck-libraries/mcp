/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * The type of invoice item, indicating whether it is an inventory item, a service, or another type.
 */
export const InvoiceItemFilterInvoiceItemType = {
  Inventory: "inventory",
  Service: "service",
  Other: "other",
} as const;
/**
 * The type of invoice item, indicating whether it is an inventory item, a service, or another type.
 */
export type InvoiceItemFilterInvoiceItemType = OpenEnum<
  typeof InvoiceItemFilterInvoiceItemType
>;

export const InvoiceItemFilterInvoiceItemType$zodSchema = z.union([
  z.enum([
    "inventory",
    "service",
    "other",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe(
  "The type of invoice item, indicating whether it is an inventory item, a service, or another type.",
);

/**
 * The kind of transaction, indicating whether it is a sales transaction or a purchase transaction.
 */
export const InvoiceItemFilterTransactionType = {
  Sale: "sale",
  Purchase: "purchase",
} as const;
/**
 * The kind of transaction, indicating whether it is a sales transaction or a purchase transaction.
 */
export type InvoiceItemFilterTransactionType = OpenEnum<
  typeof InvoiceItemFilterTransactionType
>;

export const InvoiceItemFilterTransactionType$zodSchema = z.union([
  z.enum([
    "sale",
    "purchase",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe(
  "The kind of transaction, indicating whether it is a sales transaction or a purchase transaction.",
);

export type InvoiceItemFilter = {
  type?: InvoiceItemFilterInvoiceItemType | null | undefined;
  transaction_type?: InvoiceItemFilterTransactionType | null | undefined;
};

export const InvoiceItemFilter$zodSchema: z.ZodType<InvoiceItemFilter> = z
  .object({
    transaction_type: InvoiceItemFilterTransactionType$zodSchema.nullable()
      .optional().describe(
        "The kind of transaction, indicating whether it is a sales transaction or a purchase transaction.",
      ),
    type: InvoiceItemFilterInvoiceItemType$zodSchema.nullable().optional()
      .describe(
        "The type of invoice item, indicating whether it is an inventory item, a service, or another type.",
      ),
  });
