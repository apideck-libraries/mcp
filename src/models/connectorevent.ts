/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * Unify event source
 */
export const EventSource = {
  Native: "native",
  Virtual: "virtual",
} as const;
/**
 * Unify event source
 */
export type EventSource = OpenEnum<typeof EventSource>;

export const EventSource$zodSchema = z.union([
  z.enum([
    "native",
    "virtual",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe("Unify event source");

/**
 * Unify event that is supported on the connector. Events are delivered via Webhooks.
 */
export type ConnectorEvent = {
  event_type?: string | undefined;
  event_source?: EventSource | undefined;
  downstream_event_type?: string | undefined;
  resources?: Array<string> | undefined;
  entity_type?: string | undefined;
};

export const ConnectorEvent$zodSchema: z.ZodType<ConnectorEvent> = z.object({
  downstream_event_type: z.string().optional().describe(
    "Downstream event type",
  ),
  entity_type: z.string().optional().describe("Unify entity type"),
  event_source: EventSource$zodSchema.optional().describe("Unify event source"),
  event_type: z.string().optional().describe("Unify event type"),
  resources: z.array(z.string()).optional(),
}).describe(
  "Unify event that is supported on the connector. Events are delivered via Webhooks.",
);
