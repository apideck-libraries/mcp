/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";

export type Credentials = {
  refresh_token?: string | null | undefined;
  access_token?: string | undefined;
  issued_at?: string | null | undefined;
  expires_in?: number | null | undefined;
};

export const Credentials$zodSchema: z.ZodType<Credentials> = z.object({
  access_token: z.string().optional().describe("Access token"),
  expires_in: z.int().nullable().optional().describe(
    "The number of seconds until the token expires. If omitted the token will be queued for refresh.",
  ),
  issued_at: z.iso.datetime({ offset: true }).nullable().optional().describe(
    "The datetime at which the token was issued. If omitted the token will be queued for refresh.",
  ),
  refresh_token: z.string().nullable().optional().describe(
    "The refresh token can be used to obtain a new access token.",
  ),
});

export type ConnectionImportData = {
  credentials?: Credentials | undefined;
  settings?: { [k: string]: any } | null | undefined;
  metadata?: { [k: string]: any } | null | undefined;
};

export const ConnectionImportData$zodSchema: z.ZodType<ConnectionImportData> = z
  .object({
    credentials: z.lazy(() => Credentials$zodSchema).optional(),
    metadata: z.record(z.string(), z.any()).nullable().optional().describe(
      "Attach your own consumer specific metadata",
    ),
    settings: z.record(z.string(), z.any()).nullable().optional().describe(
      "Connection settings. Values will persist to `form_fields` with corresponding id",
    ),
  });
