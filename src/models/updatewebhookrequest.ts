/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { Status, Status$zodSchema } from "./status.js";
import {
  WebhookEventType,
  WebhookEventType$zodSchema,
} from "./webhookeventtype.js";

export type UpdateWebhookRequest = {
  description?: string | null | undefined;
  status?: Status | undefined;
  delivery_url?: string | undefined;
  events?: Array<WebhookEventType> | undefined;
};

export const UpdateWebhookRequest$zodSchema: z.ZodType<UpdateWebhookRequest> = z
  .object({
    delivery_url: z.string().optional().describe(
      "The delivery url of the webhook endpoint.",
    ),
    description: z.string().nullable().optional().describe(
      "A description of the object.",
    ),
    events: z.array(WebhookEventType$zodSchema).optional().describe(
      "The list of subscribed events for this webhook. [`*`] indicates that all events are enabled.",
    ),
    status: Status$zodSchema.optional().describe("The status of the webhook."),
  });
