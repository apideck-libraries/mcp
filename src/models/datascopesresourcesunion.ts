/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { catchUnrecognizedEnum, OpenEnum } from "../types/enums.js";

/**
 * Wildcard indicating all resources and fields when Data Scopes is selected
 */
export const DataScopesResourcesEnum = {
  Wildcard: "*",
} as const;
/**
 * Wildcard indicating all resources and fields when Data Scopes is selected
 */
export type DataScopesResourcesEnum = OpenEnum<typeof DataScopesResourcesEnum>;

export const DataScopesResourcesEnum$zodSchema = z.union([
  z.enum([
    "*",
  ]),
  z.string().transform(catchUnrecognizedEnum),
]).describe(
  "Wildcard indicating all resources and fields when Data Scopes is selected",
);

export type DataScopesResources = {
  read?: boolean | undefined;
  write?: boolean | undefined;
};

export const DataScopesResources$zodSchema: z.ZodType<DataScopesResources> = z
  .object({
    read: z.boolean().optional().describe("Whether the field can be read"),
    write: z.boolean().optional().describe(
      "Whether the field can be written to",
    ),
  });

/**
 * Data scopes resource configuration that can be either detailed field permissions or a wildcard
 */
export type DataScopesResourcesUnion = {
  [k: string]: { [k: string]: DataScopesResources };
} | DataScopesResourcesEnum;

export const DataScopesResourcesUnion$zodSchema: z.ZodType<
  DataScopesResourcesUnion
> = z.union([
  z.record(
    z.string(),
    z.record(z.string(), z.lazy(() => DataScopesResources$zodSchema)),
  ),
  DataScopesResourcesEnum$zodSchema,
]).describe(
  "Data scopes resource configuration that can be either detailed field permissions or a wildcard",
);
