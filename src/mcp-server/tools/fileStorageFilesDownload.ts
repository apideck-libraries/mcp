/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFilesDownload } from "../../funcs/fileStorageFilesDownload.js";
import { FileStorageFilesDownloadRequest$zodSchema } from "../../models/filestoragefilesdownloadop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFilesDownloadRequest$zodSchema,
};

export const tool$fileStorageFilesDownload: ToolDefinition<typeof args> = {
  name: "file-storage-files-download",
  description: `Download File

Download File`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFilesDownload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
