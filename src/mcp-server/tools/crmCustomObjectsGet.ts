/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmCustomObjectsGet } from "../../funcs/crmCustomObjectsGet.js";
import { CrmCustomObjectsOneRequest$zodSchema } from "../../models/crmcustomobjectsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmCustomObjectsOneRequest$zodSchema,
};

export const tool$crmCustomObjectsGet: ToolDefinition<typeof args> = {
  name: "crm-custom-objects-get",
  description: `Get custom object

Get custom object`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmCustomObjectsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
