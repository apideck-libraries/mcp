/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingLocationsDelete } from "../../funcs/accountingLocationsDelete.js";
import { AccountingLocationsDeleteRequest$zodSchema } from "../../models/accountinglocationsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingLocationsDeleteRequest$zodSchema,
};

export const tool$accountingLocationsDelete: ToolDefinition<typeof args> = {
  name: "accounting-locations-delete",
  description: `Delete Location

Delete Location`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingLocationsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
