/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingProjectsGet } from "../../funcs/accountingProjectsGet.js";
import { AccountingProjectsOneRequest$zodSchema } from "../../models/accountingprojectsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingProjectsOneRequest$zodSchema,
};

export const tool$accountingProjectsGet: ToolDefinition<typeof args> = {
  name: "accounting-projects-get",
  description: `Get project

Get project`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingProjectsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
