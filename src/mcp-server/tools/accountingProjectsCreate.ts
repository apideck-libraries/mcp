/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingProjectsCreate } from "../../funcs/accountingProjectsCreate.js";
import { AccountingProjectsAddRequest$zodSchema } from "../../models/accountingprojectsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingProjectsAddRequest$zodSchema,
};

export const tool$accountingProjectsCreate: ToolDefinition<typeof args> = {
  name: "accounting-projects-create",
  description: `Create project

Create project`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingProjectsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
