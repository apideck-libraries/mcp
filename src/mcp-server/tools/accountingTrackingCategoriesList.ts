/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingTrackingCategoriesList } from "../../funcs/accountingTrackingCategoriesList.js";
import { AccountingTrackingCategoriesAllRequest$zodSchema } from "../../models/accountingtrackingcategoriesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingTrackingCategoriesAllRequest$zodSchema.optional(),
};

export const tool$accountingTrackingCategoriesList: ToolDefinition<
  typeof args
> = {
  name: "accounting-tracking-categories-list",
  description: `List Tracking Categories

List Tracking Categories`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingTrackingCategoriesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
