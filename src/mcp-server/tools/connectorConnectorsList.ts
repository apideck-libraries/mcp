/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorConnectorsList } from "../../funcs/connectorConnectorsList.js";
import { ConnectorConnectorsAllRequest$zodSchema } from "../../models/connectorconnectorsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectorConnectorsAllRequest$zodSchema.optional(),
};

export const tool$connectorConnectorsList: ToolDefinition<typeof args> = {
  name: "connector-connectors-list",
  description: `List Connectors

List Connectors`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorConnectorsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
