/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingTaxRatesDelete } from "../../funcs/accountingTaxRatesDelete.js";
import { AccountingTaxRatesDeleteRequest$zodSchema } from "../../models/accountingtaxratesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingTaxRatesDeleteRequest$zodSchema,
};

export const tool$accountingTaxRatesDelete: ToolDefinition<typeof args> = {
  name: "accounting-tax-rates-delete",
  description: `Delete Tax Rate

Delete Tax Rate`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingTaxRatesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
