/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingLocationsCreate } from "../../funcs/accountingLocationsCreate.js";
import { AccountingLocationsAddRequest$zodSchema } from "../../models/accountinglocationsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingLocationsAddRequest$zodSchema,
};

export const tool$accountingLocationsCreate: ToolDefinition<typeof args> = {
  name: "accounting-locations-create",
  description: `Create Location

Create Location`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingLocationsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
