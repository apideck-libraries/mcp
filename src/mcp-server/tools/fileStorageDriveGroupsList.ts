/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageDriveGroupsList } from "../../funcs/fileStorageDriveGroupsList.js";
import { FileStorageDriveGroupsAllRequest$zodSchema } from "../../models/filestoragedrivegroupsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageDriveGroupsAllRequest$zodSchema.optional(),
};

export const tool$fileStorageDriveGroupsList: ToolDefinition<typeof args> = {
  name: "file-storage-drive-groups-list",
  description: `List DriveGroups

List DriveGroups`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageDriveGroupsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
