/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingSubsidiariesDelete } from "../../funcs/accountingSubsidiariesDelete.js";
import { AccountingSubsidiariesDeleteRequest$zodSchema } from "../../models/accountingsubsidiariesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingSubsidiariesDeleteRequest$zodSchema,
};

export const tool$accountingSubsidiariesDelete: ToolDefinition<typeof args> = {
  name: "accounting-subsidiaries-delete",
  description: `Delete Subsidiary

Delete Subsidiary`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingSubsidiariesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
