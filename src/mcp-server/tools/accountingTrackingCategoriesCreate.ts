/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingTrackingCategoriesCreate } from "../../funcs/accountingTrackingCategoriesCreate.js";
import { AccountingTrackingCategoriesAddRequest$zodSchema } from "../../models/accountingtrackingcategoriesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingTrackingCategoriesAddRequest$zodSchema,
};

export const tool$accountingTrackingCategoriesCreate: ToolDefinition<
  typeof args
> = {
  name: "accounting-tracking-categories-create",
  description: `Create Tracking Category

Create Tracking Category`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingTrackingCategoriesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
