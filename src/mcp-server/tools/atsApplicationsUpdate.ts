/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsApplicationsUpdate } from "../../funcs/atsApplicationsUpdate.js";
import { AtsApplicationsUpdateRequest$zodSchema } from "../../models/atsapplicationsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AtsApplicationsUpdateRequest$zodSchema,
};

export const tool$atsApplicationsUpdate: ToolDefinition<typeof args> = {
  name: "ats-applications-update",
  description: `Update Application

Update Application`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await atsApplicationsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
