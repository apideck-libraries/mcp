/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ecommerceProductsList } from "../../funcs/ecommerceProductsList.js";
import { EcommerceProductsAllRequest$zodSchema } from "../../models/ecommerceproductsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EcommerceProductsAllRequest$zodSchema.optional(),
};

export const tool$ecommerceProductsList: ToolDefinition<typeof args> = {
  name: "ecommerce-products-list",
  description: `List Products

List Products`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ecommerceProductsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
