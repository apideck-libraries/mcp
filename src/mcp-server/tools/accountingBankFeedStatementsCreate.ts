/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingBankFeedStatementsCreate } from "../../funcs/accountingBankFeedStatementsCreate.js";
import { AccountingBankFeedStatementsAddRequest$zodSchema } from "../../models/accountingbankfeedstatementsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingBankFeedStatementsAddRequest$zodSchema,
};

export const tool$accountingBankFeedStatementsCreate: ToolDefinition<
  typeof args
> = {
  name: "accounting-bank-feed-statements-create",
  description: `Create Bank Feed Statement

Create Bank Feed Statement`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingBankFeedStatementsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
