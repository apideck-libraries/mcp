/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoiceItemsDelete } from "../../funcs/accountingInvoiceItemsDelete.js";
import { AccountingInvoiceItemsDeleteRequest$zodSchema } from "../../models/accountinginvoiceitemsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoiceItemsDeleteRequest$zodSchema,
};

export const tool$accountingInvoiceItemsDelete: ToolDefinition<typeof args> = {
  name: "accounting-invoice-items-delete",
  description: `Delete Invoice Item

Delete Invoice Item`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoiceItemsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
