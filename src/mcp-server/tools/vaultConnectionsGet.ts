/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultConnectionsGet } from "../../funcs/vaultConnectionsGet.js";
import { VaultConnectionsOneRequest$zodSchema } from "../../models/vaultconnectionsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultConnectionsOneRequest$zodSchema,
};

export const tool$vaultConnectionsGet: ToolDefinition<typeof args> = {
  name: "vault-connections-get",
  description: `Get connection

Get a connection`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultConnectionsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
