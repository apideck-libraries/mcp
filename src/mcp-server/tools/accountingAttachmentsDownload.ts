/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingAttachmentsDownload } from "../../funcs/accountingAttachmentsDownload.js";
import { AccountingAttachmentsDownloadRequest$zodSchema } from "../../models/accountingattachmentsdownloadop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingAttachmentsDownloadRequest$zodSchema,
};

export const tool$accountingAttachmentsDownload: ToolDefinition<typeof args> = {
  name: "accounting-attachments-download",
  description: `Download Attachment

Download Attachment`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingAttachmentsDownload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
