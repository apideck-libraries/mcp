/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { proxyPost } from "../../funcs/proxyPost.js";
import { ProxyPostProxyRequest$zodSchema } from "../../models/proxypostproxyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ProxyPostProxyRequest$zodSchema,
};

export const tool$proxyPost: ToolDefinition<typeof args> = {
  name: "proxy-post",
  description: `POST

Proxies a downstream POST request to a service and injects the necessary credentials into a request stored in Vault. This allows you to have an additional security layer and logging without needing to rely on Unify for normalization.
**Note**: Vault will proxy all data to the downstream URL and method/verb in the headers.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await proxyPost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
