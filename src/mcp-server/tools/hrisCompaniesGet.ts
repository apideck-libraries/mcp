/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisCompaniesGet } from "../../funcs/hrisCompaniesGet.js";
import { HrisCompaniesOneRequest$zodSchema } from "../../models/hriscompaniesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HrisCompaniesOneRequest$zodSchema,
};

export const tool$hrisCompaniesGet: ToolDefinition<typeof args> = {
  name: "hris-companies-get",
  description: `Get Company

Get Company`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await hrisCompaniesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
