/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingTrackingCategoriesDelete } from "../../funcs/accountingTrackingCategoriesDelete.js";
import { AccountingTrackingCategoriesDeleteRequest$zodSchema } from "../../models/accountingtrackingcategoriesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingTrackingCategoriesDeleteRequest$zodSchema,
};

export const tool$accountingTrackingCategoriesDelete: ToolDefinition<
  typeof args
> = {
  name: "accounting-tracking-categories-delete",
  description: `Delete Tracking Category

Delete Tracking Category`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingTrackingCategoriesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
