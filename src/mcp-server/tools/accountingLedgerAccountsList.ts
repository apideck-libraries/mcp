/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingLedgerAccountsList } from "../../funcs/accountingLedgerAccountsList.js";
import { AccountingLedgerAccountsAllRequest$zodSchema } from "../../models/accountingledgeraccountsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingLedgerAccountsAllRequest$zodSchema.optional(),
};

export const tool$accountingLedgerAccountsList: ToolDefinition<typeof args> = {
  name: "accounting-ledger-accounts-list",
  description: `List Ledger Accounts

List Ledger Accounts`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingLedgerAccountsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
