/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { issueTrackingCollectionTicketCommentsList } from "../../funcs/issueTrackingCollectionTicketCommentsList.js";
import { IssueTrackingCollectionTicketCommentsAllRequest$zodSchema } from "../../models/issuetrackingcollectionticketcommentsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IssueTrackingCollectionTicketCommentsAllRequest$zodSchema,
};

export const tool$issueTrackingCollectionTicketCommentsList: ToolDefinition<
  typeof args
> = {
  name: "issue-tracking-collection-ticket-comments-list",
  description: `List Comments

List Comments`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await issueTrackingCollectionTicketCommentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
