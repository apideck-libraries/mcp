/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPurchaseOrdersUpdate } from "../../funcs/accountingPurchaseOrdersUpdate.js";
import { AccountingPurchaseOrdersUpdateRequest$zodSchema } from "../../models/accountingpurchaseordersupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingPurchaseOrdersUpdateRequest$zodSchema,
};

export const tool$accountingPurchaseOrdersUpdate: ToolDefinition<typeof args> =
  {
    name: "accounting-purchase-orders-update",
    description: `Update Purchase Order

Update Purchase Order`,
    scopes: ["write"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await accountingPurchaseOrdersUpdate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
