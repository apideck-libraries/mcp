/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageUploadSessionsDelete } from "../../funcs/fileStorageUploadSessionsDelete.js";
import { FileStorageUploadSessionsDeleteRequest$zodSchema } from "../../models/filestorageuploadsessionsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageUploadSessionsDeleteRequest$zodSchema,
};

export const tool$fileStorageUploadSessionsDelete: ToolDefinition<typeof args> =
  {
    name: "file-storage-upload-sessions-delete",
    description: `Abort Upload Session

Abort Upload Session. Note that the base URL is upload.apideck.com instead of unify.apideck.com. For more information on uploads, refer to the [file upload guide](/guides/file-upload).`,
    scopes: ["destructive"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await fileStorageUploadSessionsDelete(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
