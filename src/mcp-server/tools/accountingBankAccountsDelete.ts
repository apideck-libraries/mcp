/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingBankAccountsDelete } from "../../funcs/accountingBankAccountsDelete.js";
import { AccountingBankAccountsDeleteRequest$zodSchema } from "../../models/accountingbankaccountsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingBankAccountsDeleteRequest$zodSchema,
};

export const tool$accountingBankAccountsDelete: ToolDefinition<typeof args> = {
  name: "accounting-bank-accounts-delete",
  description: `Delete Bank Account

Delete Bank Account`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingBankAccountsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
