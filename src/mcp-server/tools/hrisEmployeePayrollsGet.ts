/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisEmployeePayrollsGet } from "../../funcs/hrisEmployeePayrollsGet.js";
import { HrisEmployeePayrollsOneRequest$zodSchema } from "../../models/hrisemployeepayrollsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HrisEmployeePayrollsOneRequest$zodSchema,
};

export const tool$hrisEmployeePayrollsGet: ToolDefinition<typeof args> = {
  name: "hris-employee-payrolls-get",
  description: `Get Employee Payroll

Get payroll for employee`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await hrisEmployeePayrollsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
