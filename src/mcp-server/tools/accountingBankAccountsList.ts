/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingBankAccountsList } from "../../funcs/accountingBankAccountsList.js";
import { AccountingBankAccountsAllRequest$zodSchema } from "../../models/accountingbankaccountsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingBankAccountsAllRequest$zodSchema.optional(),
};

export const tool$accountingBankAccountsList: ToolDefinition<typeof args> = {
  name: "accounting-bank-accounts-list",
  description: `List Bank Accounts

List Bank Accounts`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingBankAccountsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
