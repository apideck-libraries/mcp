/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRefundsDelete } from "../../funcs/accountingRefundsDelete.js";
import { AccountingRefundsDeleteRequest$zodSchema } from "../../models/accountingrefundsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingRefundsDeleteRequest$zodSchema,
};

export const tool$accountingRefundsDelete: ToolDefinition<typeof args> = {
  name: "accounting-refunds-delete",
  description: `Delete Refund

Delete Refund`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingRefundsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
