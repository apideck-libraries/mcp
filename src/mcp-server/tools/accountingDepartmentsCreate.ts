/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingDepartmentsCreate } from "../../funcs/accountingDepartmentsCreate.js";
import { AccountingDepartmentsAddRequest$zodSchema } from "../../models/accountingdepartmentsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingDepartmentsAddRequest$zodSchema,
};

export const tool$accountingDepartmentsCreate: ToolDefinition<typeof args> = {
  name: "accounting-departments-create",
  description: `Create Department

Create Department`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingDepartmentsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
