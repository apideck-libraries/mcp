/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPaymentsCreate } from "../../funcs/accountingPaymentsCreate.js";
import { AccountingPaymentsAddRequest$zodSchema } from "../../models/accountingpaymentsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingPaymentsAddRequest$zodSchema,
};

export const tool$accountingPaymentsCreate: ToolDefinition<typeof args> = {
  name: "accounting-payments-create",
  description: `Create Payment

Create Payment`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingPaymentsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
