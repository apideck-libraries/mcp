/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmNotesCreate } from "../../funcs/crmNotesCreate.js";
import { CrmNotesAddRequest$zodSchema } from "../../models/crmnotesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmNotesAddRequest$zodSchema,
};

export const tool$crmNotesCreate: ToolDefinition<typeof args> = {
  name: "crm-notes-create",
  description: `Create note

Create note`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmNotesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
