/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { issueTrackingCollectionTagsList } from "../../funcs/issueTrackingCollectionTagsList.js";
import { IssueTrackingCollectionTagsAllRequest$zodSchema } from "../../models/issuetrackingcollectiontagsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IssueTrackingCollectionTagsAllRequest$zodSchema,
};

export const tool$issueTrackingCollectionTagsList: ToolDefinition<typeof args> =
  {
    name: "issue-tracking-collection-tags-list",
    description: `List Tags

List Tags`,
    scopes: ["read"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await issueTrackingCollectionTagsList(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
