/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsApplicationsDelete } from "../../funcs/atsApplicationsDelete.js";
import { AtsApplicationsDeleteRequest$zodSchema } from "../../models/atsapplicationsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AtsApplicationsDeleteRequest$zodSchema,
};

export const tool$atsApplicationsDelete: ToolDefinition<typeof args> = {
  name: "ats-applications-delete",
  description: `Delete Application

Delete Application`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await atsApplicationsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
