/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorApisList } from "../../funcs/connectorApisList.js";
import { ConnectorApisAllRequest$zodSchema } from "../../models/connectorapisallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectorApisAllRequest$zodSchema.optional(),
};

export const tool$connectorApisList: ToolDefinition<typeof args> = {
  name: "connector-apis-list",
  description: `List APIs

List APIs`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorApisList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
