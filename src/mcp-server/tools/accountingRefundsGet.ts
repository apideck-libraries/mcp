/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRefundsGet } from "../../funcs/accountingRefundsGet.js";
import { AccountingRefundsOneRequest$zodSchema } from "../../models/accountingrefundsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingRefundsOneRequest$zodSchema,
};

export const tool$accountingRefundsGet: ToolDefinition<typeof args> = {
  name: "accounting-refunds-get",
  description: `Get Refund

Get Refund`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingRefundsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
