/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultCreateCallbackState } from "../../funcs/vaultCreateCallbackState.js";
import { VaultCreateCallbackStateRequest$zodSchema } from "../../models/vaultcreatecallbackstateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultCreateCallbackStateRequest$zodSchema,
};

export const tool$vaultCreateCallbackState: ToolDefinition<typeof args> = {
  name: "vault-create-callback-state",
  description: `Create Callback State

This endpoint creates a callback state that can be used to issue requests to the callback endpoint.
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultCreateCallbackState(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
