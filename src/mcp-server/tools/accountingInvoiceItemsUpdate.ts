/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoiceItemsUpdate } from "../../funcs/accountingInvoiceItemsUpdate.js";
import { AccountingInvoiceItemsUpdateRequest$zodSchema } from "../../models/accountinginvoiceitemsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoiceItemsUpdateRequest$zodSchema,
};

export const tool$accountingInvoiceItemsUpdate: ToolDefinition<typeof args> = {
  name: "accounting-invoice-items-update",
  description: `Update Invoice Item

Update Invoice Item`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoiceItemsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
