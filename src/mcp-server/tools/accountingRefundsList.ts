/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRefundsList } from "../../funcs/accountingRefundsList.js";
import { AccountingRefundsAllRequest$zodSchema } from "../../models/accountingrefundsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingRefundsAllRequest$zodSchema.optional(),
};

export const tool$accountingRefundsList: ToolDefinition<typeof args> = {
  name: "accounting-refunds-list",
  description: `List Refunds

List Refunds`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingRefundsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
