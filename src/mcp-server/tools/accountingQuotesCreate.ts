/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingQuotesCreate } from "../../funcs/accountingQuotesCreate.js";
import { AccountingQuotesAddRequest$zodSchema } from "../../models/accountingquotesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingQuotesAddRequest$zodSchema,
};

export const tool$accountingQuotesCreate: ToolDefinition<typeof args> = {
  name: "accounting-quotes-create",
  description: `Create Quote

Create Quote`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingQuotesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
