/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingCompanyInfoGet } from "../../funcs/accountingCompanyInfoGet.js";
import { AccountingCompanyInfoOneRequest$zodSchema } from "../../models/accountingcompanyinfooneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingCompanyInfoOneRequest$zodSchema.optional(),
};

export const tool$accountingCompanyInfoGet: ToolDefinition<typeof args> = {
  name: "accounting-company-info-get",
  description: `Get company info

Get company info`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingCompanyInfoGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
