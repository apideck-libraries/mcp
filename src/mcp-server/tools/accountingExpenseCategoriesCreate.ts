/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingExpenseCategoriesCreate } from "../../funcs/accountingExpenseCategoriesCreate.js";
import { AccountingExpenseCategoriesAddRequest$zodSchema } from "../../models/accountingexpensecategoriesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingExpenseCategoriesAddRequest$zodSchema,
};

export const tool$accountingExpenseCategoriesCreate: ToolDefinition<
  typeof args
> = {
  name: "accounting-expense-categories-create",
  description: `Create Expense Category

Create Expense Category`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingExpenseCategoriesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
