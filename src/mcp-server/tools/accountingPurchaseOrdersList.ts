/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPurchaseOrdersList } from "../../funcs/accountingPurchaseOrdersList.js";
import { AccountingPurchaseOrdersAllRequest$zodSchema } from "../../models/accountingpurchaseordersallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingPurchaseOrdersAllRequest$zodSchema.optional(),
};

export const tool$accountingPurchaseOrdersList: ToolDefinition<typeof args> = {
  name: "accounting-purchase-orders-list",
  description: `List Purchase Orders

List Purchase Orders`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingPurchaseOrdersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
