/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingCustomersList } from "../../funcs/accountingCustomersList.js";
import { AccountingCustomersAllRequest$zodSchema } from "../../models/accountingcustomersallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingCustomersAllRequest$zodSchema.optional(),
};

export const tool$accountingCustomersList: ToolDefinition<typeof args> = {
  name: "accounting-customers-list",
  description: `List Customers

List Customers`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingCustomersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
