/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmActivitiesGet } from "../../funcs/crmActivitiesGet.js";
import { CrmActivitiesOneRequest$zodSchema } from "../../models/crmactivitiesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmActivitiesOneRequest$zodSchema,
};

export const tool$crmActivitiesGet: ToolDefinition<typeof args> = {
  name: "crm-activities-get",
  description: `Get activity

Get activity`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmActivitiesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
