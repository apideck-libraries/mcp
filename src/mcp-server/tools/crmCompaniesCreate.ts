/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmCompaniesCreate } from "../../funcs/crmCompaniesCreate.js";
import { CrmCompaniesAddRequest$zodSchema } from "../../models/crmcompaniesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmCompaniesAddRequest$zodSchema,
};

export const tool$crmCompaniesCreate: ToolDefinition<typeof args> = {
  name: "crm-companies-create",
  description: `Create company

Create company`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmCompaniesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
