/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmLeadsGet } from "../../funcs/crmLeadsGet.js";
import { CrmLeadsOneRequest$zodSchema } from "../../models/crmleadsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmLeadsOneRequest$zodSchema,
};

export const tool$crmLeadsGet: ToolDefinition<typeof args> = {
  name: "crm-leads-get",
  description: `Get lead

Get lead`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmLeadsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
