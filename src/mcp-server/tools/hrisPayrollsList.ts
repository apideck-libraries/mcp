/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisPayrollsList } from "../../funcs/hrisPayrollsList.js";
import { HrisPayrollsAllRequest$zodSchema } from "../../models/hrispayrollsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HrisPayrollsAllRequest$zodSchema.optional(),
};

export const tool$hrisPayrollsList: ToolDefinition<typeof args> = {
  name: "hris-payrolls-list",
  description: `List Payroll

List Payroll`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await hrisPayrollsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
