/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { issueTrackingCollectionTicketsCreate } from "../../funcs/issueTrackingCollectionTicketsCreate.js";
import { IssueTrackingCollectionTicketsAddRequest$zodSchema } from "../../models/issuetrackingcollectionticketsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IssueTrackingCollectionTicketsAddRequest$zodSchema,
};

export const tool$issueTrackingCollectionTicketsCreate: ToolDefinition<
  typeof args
> = {
  name: "issue-tracking-collection-tickets-create",
  description: `Create Ticket

Create Ticket`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await issueTrackingCollectionTicketsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
