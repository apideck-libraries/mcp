/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFoldersDelete } from "../../funcs/fileStorageFoldersDelete.js";
import { FileStorageFoldersDeleteRequest$zodSchema } from "../../models/filestoragefoldersdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFoldersDeleteRequest$zodSchema,
};

export const tool$fileStorageFoldersDelete: ToolDefinition<typeof args> = {
  name: "file-storage-folders-delete",
  description: `Delete Folder

Delete Folder`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFoldersDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
