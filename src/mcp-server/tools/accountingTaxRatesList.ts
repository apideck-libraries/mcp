/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingTaxRatesList } from "../../funcs/accountingTaxRatesList.js";
import { AccountingTaxRatesAllRequest$zodSchema } from "../../models/accountingtaxratesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingTaxRatesAllRequest$zodSchema.optional(),
};

export const tool$accountingTaxRatesList: ToolDefinition<typeof args> = {
  name: "accounting-tax-rates-list",
  description: `List Tax Rates

List Tax Rates. Note: Not all connectors return the actual rate/percentage value. In this case, only the tax code or reference is returned. Connectors Affected: Quickbooks
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingTaxRatesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
