/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingBillsCreate } from "../../funcs/accountingBillsCreate.js";
import { AccountingBillsAddRequest$zodSchema } from "../../models/accountingbillsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingBillsAddRequest$zodSchema,
};

export const tool$accountingBillsCreate: ToolDefinition<typeof args> = {
  name: "accounting-bills-create",
  description: `Create Bill

Create Bill`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingBillsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
