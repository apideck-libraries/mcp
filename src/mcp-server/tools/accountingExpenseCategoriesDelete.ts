/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingExpenseCategoriesDelete } from "../../funcs/accountingExpenseCategoriesDelete.js";
import { AccountingExpenseCategoriesDeleteRequest$zodSchema } from "../../models/accountingexpensecategoriesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingExpenseCategoriesDeleteRequest$zodSchema,
};

export const tool$accountingExpenseCategoriesDelete: ToolDefinition<
  typeof args
> = {
  name: "accounting-expense-categories-delete",
  description: `Delete Expense Category

Delete Expense Category`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingExpenseCategoriesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
