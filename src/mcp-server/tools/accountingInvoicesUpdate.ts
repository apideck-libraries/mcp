/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoicesUpdate } from "../../funcs/accountingInvoicesUpdate.js";
import { AccountingInvoicesUpdateRequest$zodSchema } from "../../models/accountinginvoicesupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoicesUpdateRequest$zodSchema,
};

export const tool$accountingInvoicesUpdate: ToolDefinition<typeof args> = {
  name: "accounting-invoices-update",
  description: `Update Invoice

Update Invoice`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoicesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
