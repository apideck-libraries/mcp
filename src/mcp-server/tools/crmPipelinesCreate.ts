/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmPipelinesCreate } from "../../funcs/crmPipelinesCreate.js";
import { CrmPipelinesAddRequest$zodSchema } from "../../models/crmpipelinesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmPipelinesAddRequest$zodSchema,
};

export const tool$crmPipelinesCreate: ToolDefinition<typeof args> = {
  name: "crm-pipelines-create",
  description: `Create pipeline

Create pipeline`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmPipelinesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
