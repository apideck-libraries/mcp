/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingProjectsDelete } from "../../funcs/accountingProjectsDelete.js";
import { AccountingProjectsDeleteRequest$zodSchema } from "../../models/accountingprojectsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingProjectsDeleteRequest$zodSchema,
};

export const tool$accountingProjectsDelete: ToolDefinition<typeof args> = {
  name: "accounting-projects-delete",
  description: `Delete project

Delete project`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingProjectsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
