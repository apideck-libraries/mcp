/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultConsumersCreate } from "../../funcs/vaultConsumersCreate.js";
import { VaultConsumersAddRequest$zodSchema } from "../../models/vaultconsumersaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultConsumersAddRequest$zodSchema,
};

export const tool$vaultConsumersCreate: ToolDefinition<typeof args> = {
  name: "vault-consumers-create",
  description: `Create consumer

Create a consumer`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultConsumersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
