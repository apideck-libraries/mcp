/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmNotesGet } from "../../funcs/crmNotesGet.js";
import { CrmNotesOneRequest$zodSchema } from "../../models/crmnotesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmNotesOneRequest$zodSchema,
};

export const tool$crmNotesGet: ToolDefinition<typeof args> = {
  name: "crm-notes-get",
  description: `Get note

Get note`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmNotesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
