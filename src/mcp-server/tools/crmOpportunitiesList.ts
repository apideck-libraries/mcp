/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmOpportunitiesList } from "../../funcs/crmOpportunitiesList.js";
import { CrmOpportunitiesAllRequest$zodSchema } from "../../models/crmopportunitiesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmOpportunitiesAllRequest$zodSchema.optional(),
};

export const tool$crmOpportunitiesList: ToolDefinition<typeof args> = {
  name: "crm-opportunities-list",
  description: `List opportunities

List opportunities`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmOpportunitiesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
