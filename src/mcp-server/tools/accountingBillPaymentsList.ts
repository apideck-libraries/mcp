/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingBillPaymentsList } from "../../funcs/accountingBillPaymentsList.js";
import { AccountingBillPaymentsAllRequest$zodSchema } from "../../models/accountingbillpaymentsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingBillPaymentsAllRequest$zodSchema.optional(),
};

export const tool$accountingBillPaymentsList: ToolDefinition<typeof args> = {
  name: "accounting-bill-payments-list",
  description: `List Bill Payments

List Bill Payments`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingBillPaymentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
