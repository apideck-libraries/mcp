/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingSubsidiariesList } from "../../funcs/accountingSubsidiariesList.js";
import { AccountingSubsidiariesAllRequest$zodSchema } from "../../models/accountingsubsidiariesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingSubsidiariesAllRequest$zodSchema.optional(),
};

export const tool$accountingSubsidiariesList: ToolDefinition<typeof args> = {
  name: "accounting-subsidiaries-list",
  description: `List Subsidiaries

List Subsidiaries`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingSubsidiariesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
