/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultConnectionsList } from "../../funcs/vaultConnectionsList.js";
import { VaultConnectionsAllRequest$zodSchema } from "../../models/vaultconnectionsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultConnectionsAllRequest$zodSchema.optional(),
};

export const tool$vaultConnectionsList: ToolDefinition<typeof args> = {
  name: "vault-connections-list",
  description: `Get all connections

This endpoint includes all the configured integrations and contains the required assets
to build an integrations page where your users can install integrations.
OAuth2 supported integrations will contain authorize and revoke links to handle the authentication flows.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultConnectionsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
