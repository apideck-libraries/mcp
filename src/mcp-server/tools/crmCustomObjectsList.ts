/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmCustomObjectsList } from "../../funcs/crmCustomObjectsList.js";
import { CrmCustomObjectsAllRequest$zodSchema } from "../../models/crmcustomobjectsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmCustomObjectsAllRequest$zodSchema,
};

export const tool$crmCustomObjectsList: ToolDefinition<typeof args> = {
  name: "crm-custom-objects-list",
  description: `List custom objects

List custom objects`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmCustomObjectsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
