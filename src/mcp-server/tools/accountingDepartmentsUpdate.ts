/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingDepartmentsUpdate } from "../../funcs/accountingDepartmentsUpdate.js";
import { AccountingDepartmentsUpdateRequest$zodSchema } from "../../models/accountingdepartmentsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingDepartmentsUpdateRequest$zodSchema,
};

export const tool$accountingDepartmentsUpdate: ToolDefinition<typeof args> = {
  name: "accounting-departments-update",
  description: `Update Department

Update Department`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingDepartmentsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
