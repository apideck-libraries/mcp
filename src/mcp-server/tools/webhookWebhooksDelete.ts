/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhookWebhooksDelete } from "../../funcs/webhookWebhooksDelete.js";
import { WebhookWebhooksDeleteRequest$zodSchema } from "../../models/webhookwebhooksdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: WebhookWebhooksDeleteRequest$zodSchema,
};

export const tool$webhookWebhooksDelete: ToolDefinition<typeof args> = {
  name: "webhook-webhooks-delete",
  description: `Delete webhook subscription

Delete a webhook subscription`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await webhookWebhooksDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
