/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmContactsDelete } from "../../funcs/crmContactsDelete.js";
import { CrmContactsDeleteRequest$zodSchema } from "../../models/crmcontactsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmContactsDeleteRequest$zodSchema,
};

export const tool$crmContactsDelete: ToolDefinition<typeof args> = {
  name: "crm-contacts-delete",
  description: `Delete contact

Delete contact`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmContactsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
