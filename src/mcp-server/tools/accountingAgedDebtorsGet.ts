/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingAgedDebtorsGet } from "../../funcs/accountingAgedDebtorsGet.js";
import { AccountingAgedDebtorsOneRequest$zodSchema } from "../../models/accountingageddebtorsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingAgedDebtorsOneRequest$zodSchema.optional(),
};

export const tool$accountingAgedDebtorsGet: ToolDefinition<typeof args> = {
  name: "accounting-aged-debtors-get",
  description: `Get Aged Debtors

Get Aged Debtors`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingAgedDebtorsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
