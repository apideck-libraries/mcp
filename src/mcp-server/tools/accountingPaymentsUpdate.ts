/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingPaymentsUpdate } from "../../funcs/accountingPaymentsUpdate.js";
import { AccountingPaymentsUpdateRequest$zodSchema } from "../../models/accountingpaymentsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingPaymentsUpdateRequest$zodSchema,
};

export const tool$accountingPaymentsUpdate: ToolDefinition<typeof args> = {
  name: "accounting-payments-update",
  description: `Update Payment

Update Payment`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingPaymentsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
