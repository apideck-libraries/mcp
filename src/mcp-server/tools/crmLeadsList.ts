/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmLeadsList } from "../../funcs/crmLeadsList.js";
import { CrmLeadsAllRequest$zodSchema } from "../../models/crmleadsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmLeadsAllRequest$zodSchema.optional(),
};

export const tool$crmLeadsList: ToolDefinition<typeof args> = {
  name: "crm-leads-list",
  description: `List leads

List leads`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmLeadsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
