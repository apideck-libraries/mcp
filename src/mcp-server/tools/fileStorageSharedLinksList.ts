/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageSharedLinksList } from "../../funcs/fileStorageSharedLinksList.js";
import { FileStorageSharedLinksAllRequest$zodSchema } from "../../models/filestoragesharedlinksallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageSharedLinksAllRequest$zodSchema.optional(),
};

export const tool$fileStorageSharedLinksList: ToolDefinition<typeof args> = {
  name: "file-storage-shared-links-list",
  description: `List Shared Links

List Shared Links`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageSharedLinksList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
