/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsApplicantsCreate } from "../../funcs/atsApplicantsCreate.js";
import { AtsApplicantsAddRequest$zodSchema } from "../../models/atsapplicantsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AtsApplicantsAddRequest$zodSchema,
};

export const tool$atsApplicantsCreate: ToolDefinition<typeof args> = {
  name: "ats-applicants-create",
  description: `Create Applicant

Create Applicant`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await atsApplicantsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
