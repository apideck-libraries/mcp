/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageDrivesGet } from "../../funcs/fileStorageDrivesGet.js";
import { FileStorageDrivesOneRequest$zodSchema } from "../../models/filestoragedrivesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageDrivesOneRequest$zodSchema,
};

export const tool$fileStorageDrivesGet: ToolDefinition<typeof args> = {
  name: "file-storage-drives-get",
  description: `Get Drive

Get Drive`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageDrivesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
