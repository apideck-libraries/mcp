/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingRefundsCreate } from "../../funcs/accountingRefundsCreate.js";
import { AccountingRefundsAddRequest$zodSchema } from "../../models/accountingrefundsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingRefundsAddRequest$zodSchema,
};

export const tool$accountingRefundsCreate: ToolDefinition<typeof args> = {
  name: "accounting-refunds-create",
  description: `Create Refund

Create Refund`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingRefundsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
