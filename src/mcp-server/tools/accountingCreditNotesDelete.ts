/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingCreditNotesDelete } from "../../funcs/accountingCreditNotesDelete.js";
import { AccountingCreditNotesDeleteRequest$zodSchema } from "../../models/accountingcreditnotesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingCreditNotesDeleteRequest$zodSchema,
};

export const tool$accountingCreditNotesDelete: ToolDefinition<typeof args> = {
  name: "accounting-credit-notes-delete",
  description: `Delete Credit Note

Delete Credit Note`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingCreditNotesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
