/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingExpensesDelete } from "../../funcs/accountingExpensesDelete.js";
import { AccountingExpensesDeleteRequest$zodSchema } from "../../models/accountingexpensesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingExpensesDeleteRequest$zodSchema,
};

export const tool$accountingExpensesDelete: ToolDefinition<typeof args> = {
  name: "accounting-expenses-delete",
  description: `Delete Expense

Delete Expense`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingExpensesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
