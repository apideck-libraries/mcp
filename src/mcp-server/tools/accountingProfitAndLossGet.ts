/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingProfitAndLossGet } from "../../funcs/accountingProfitAndLossGet.js";
import { AccountingProfitAndLossOneRequest$zodSchema } from "../../models/accountingprofitandlossoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingProfitAndLossOneRequest$zodSchema.optional(),
};

export const tool$accountingProfitAndLossGet: ToolDefinition<typeof args> = {
  name: "accounting-profit-and-loss-get",
  description: `Get Profit and Loss

Get Profit and Loss`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingProfitAndLossGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
