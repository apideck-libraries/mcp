/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingSuppliersList } from "../../funcs/accountingSuppliersList.js";
import { AccountingSuppliersAllRequest$zodSchema } from "../../models/accountingsuppliersallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingSuppliersAllRequest$zodSchema.optional(),
};

export const tool$accountingSuppliersList: ToolDefinition<typeof args> = {
  name: "accounting-suppliers-list",
  description: `List Suppliers

List Suppliers`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingSuppliersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
