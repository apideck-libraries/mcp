/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmPipelinesDelete } from "../../funcs/crmPipelinesDelete.js";
import { CrmPipelinesDeleteRequest$zodSchema } from "../../models/crmpipelinesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmPipelinesDeleteRequest$zodSchema,
};

export const tool$crmPipelinesDelete: ToolDefinition<typeof args> = {
  name: "crm-pipelines-delete",
  description: `Delete pipeline

Delete pipeline`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmPipelinesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
