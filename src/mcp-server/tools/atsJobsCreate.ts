/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsJobsCreate } from "../../funcs/atsJobsCreate.js";
import { AtsJobsAddRequest$zodSchema } from "../../models/atsjobsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AtsJobsAddRequest$zodSchema,
};

export const tool$atsJobsCreate: ToolDefinition<typeof args> = {
  name: "ats-jobs-create",
  description: `Create Job

Create Job`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await atsJobsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
