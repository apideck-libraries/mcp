/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingBankFeedAccountsUpdate } from "../../funcs/accountingBankFeedAccountsUpdate.js";
import { AccountingBankFeedAccountsUpdateRequest$zodSchema } from "../../models/accountingbankfeedaccountsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingBankFeedAccountsUpdateRequest$zodSchema,
};

export const tool$accountingBankFeedAccountsUpdate: ToolDefinition<
  typeof args
> = {
  name: "accounting-bank-feed-accounts-update",
  description: `Update Bank Feed Account

Update Bank Feed Account`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingBankFeedAccountsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
