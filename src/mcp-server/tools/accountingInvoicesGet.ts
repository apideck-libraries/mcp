/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoicesGet } from "../../funcs/accountingInvoicesGet.js";
import { AccountingInvoicesOneRequest$zodSchema } from "../../models/accountinginvoicesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoicesOneRequest$zodSchema,
};

export const tool$accountingInvoicesGet: ToolDefinition<typeof args> = {
  name: "accounting-invoices-get",
  description: `Get Invoice

Get Invoice`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoicesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
