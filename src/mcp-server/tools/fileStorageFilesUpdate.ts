/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFilesUpdate } from "../../funcs/fileStorageFilesUpdate.js";
import { FileStorageFilesUpdateRequest$zodSchema } from "../../models/filestoragefilesupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFilesUpdateRequest$zodSchema,
};

export const tool$fileStorageFilesUpdate: ToolDefinition<typeof args> = {
  name: "file-storage-files-update",
  description: `Rename or move File

Rename or move File`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFilesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
