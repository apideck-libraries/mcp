/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisPayrollsGet } from "../../funcs/hrisPayrollsGet.js";
import { HrisPayrollsOneRequest$zodSchema } from "../../models/hrispayrollsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HrisPayrollsOneRequest$zodSchema,
};

export const tool$hrisPayrollsGet: ToolDefinition<typeof args> = {
  name: "hris-payrolls-get",
  description: `Get Payroll

Get Payroll`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await hrisPayrollsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
