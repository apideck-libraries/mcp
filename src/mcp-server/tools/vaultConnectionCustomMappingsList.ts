/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultConnectionCustomMappingsList } from "../../funcs/vaultConnectionCustomMappingsList.js";
import { VaultConnectionCustomMappingsAllRequest$zodSchema } from "../../models/vaultconnectioncustommappingsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultConnectionCustomMappingsAllRequest$zodSchema,
};

export const tool$vaultConnectionCustomMappingsList: ToolDefinition<
  typeof args
> = {
  name: "vault-connection-custom-mappings-list",
  description: `List connection custom mappings

This endpoint returns a list of custom mappings for a connection.`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultConnectionCustomMappingsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
