/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingExpenseReportsList } from "../../funcs/accountingExpenseReportsList.js";
import { AccountingExpenseReportsAllRequest$zodSchema } from "../../models/accountingexpensereportsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingExpenseReportsAllRequest$zodSchema.optional(),
};

export const tool$accountingExpenseReportsList: ToolDefinition<typeof args> = {
  name: "accounting-expense-reports-list",
  description: `List Expense Reports

List Expense Reports`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingExpenseReportsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
