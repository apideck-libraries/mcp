/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmCustomObjectsDelete } from "../../funcs/crmCustomObjectsDelete.js";
import { CrmCustomObjectsDeleteRequest$zodSchema } from "../../models/crmcustomobjectsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmCustomObjectsDeleteRequest$zodSchema,
};

export const tool$crmCustomObjectsDelete: ToolDefinition<typeof args> = {
  name: "crm-custom-objects-delete",
  description: `Delete custom object

Delete custom object`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmCustomObjectsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
