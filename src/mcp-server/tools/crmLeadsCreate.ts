/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmLeadsCreate } from "../../funcs/crmLeadsCreate.js";
import { CrmLeadsAddRequest$zodSchema } from "../../models/crmleadsaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmLeadsAddRequest$zodSchema,
};

export const tool$crmLeadsCreate: ToolDefinition<typeof args> = {
  name: "crm-leads-create",
  description: `Create lead

Create lead`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmLeadsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
