/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingAttachmentsDelete } from "../../funcs/accountingAttachmentsDelete.js";
import { AccountingAttachmentsDeleteRequest$zodSchema } from "../../models/accountingattachmentsdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingAttachmentsDeleteRequest$zodSchema,
};

export const tool$accountingAttachmentsDelete: ToolDefinition<typeof args> = {
  name: "accounting-attachments-delete",
  description: `Delete Attachment

Delete Attachment`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingAttachmentsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
