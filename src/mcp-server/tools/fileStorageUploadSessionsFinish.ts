/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageUploadSessionsFinish } from "../../funcs/fileStorageUploadSessionsFinish.js";
import { FileStorageUploadSessionsFinishRequest$zodSchema } from "../../models/filestorageuploadsessionsfinishop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageUploadSessionsFinishRequest$zodSchema,
};

export const tool$fileStorageUploadSessionsFinish: ToolDefinition<typeof args> =
  {
    name: "file-storage-upload-sessions-finish",
    description: `Finish Upload Session

Finish Upload Session. Only call this endpoint after all File parts have been uploaded to [Upload part of File](#operation/uploadSessionsUpload). Note that the base URL is upload.apideck.com instead of unify.apideck.com. For more information on uploads, refer to the [file upload guide](/guides/file-upload).`,
    scopes: ["write"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await fileStorageUploadSessionsFinish(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
