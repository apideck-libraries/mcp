/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingExpensesCreate } from "../../funcs/accountingExpensesCreate.js";
import { AccountingExpensesAddRequest$zodSchema } from "../../models/accountingexpensesaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingExpensesAddRequest$zodSchema,
};

export const tool$accountingExpensesCreate: ToolDefinition<typeof args> = {
  name: "accounting-expenses-create",
  description: `Create Expense

Create Expense`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingExpensesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
