/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingCustomersCreate } from "../../funcs/accountingCustomersCreate.js";
import { AccountingCustomersAddRequest$zodSchema } from "../../models/accountingcustomersaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingCustomersAddRequest$zodSchema,
};

export const tool$accountingCustomersCreate: ToolDefinition<typeof args> = {
  name: "accounting-customers-create",
  description: `Create Customer

Create Customer`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingCustomersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
