/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultCustomFieldsList } from "../../funcs/vaultCustomFieldsList.js";
import { VaultCustomFieldsAllRequest$zodSchema } from "../../models/vaultcustomfieldsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultCustomFieldsAllRequest$zodSchema,
};

export const tool$vaultCustomFieldsList: ToolDefinition<typeof args> = {
  name: "vault-custom-fields-list",
  description: `Get resource custom fields

This endpoint returns an custom fields on a connection resource.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultCustomFieldsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
