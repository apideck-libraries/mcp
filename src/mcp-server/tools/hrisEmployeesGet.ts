/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisEmployeesGet } from "../../funcs/hrisEmployeesGet.js";
import { HrisEmployeesOneRequest$zodSchema } from "../../models/hrisemployeesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HrisEmployeesOneRequest$zodSchema,
};

export const tool$hrisEmployeesGet: ToolDefinition<typeof args> = {
  name: "hris-employees-get",
  description: `Get Employee

Get Employee`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await hrisEmployeesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
