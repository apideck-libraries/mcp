/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingSuppliersUpdate } from "../../funcs/accountingSuppliersUpdate.js";
import { AccountingSuppliersUpdateRequest$zodSchema } from "../../models/accountingsuppliersupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingSuppliersUpdateRequest$zodSchema,
};

export const tool$accountingSuppliersUpdate: ToolDefinition<typeof args> = {
  name: "accounting-suppliers-update",
  description: `Update Supplier

Update Supplier`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingSuppliersUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
