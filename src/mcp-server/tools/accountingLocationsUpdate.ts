/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingLocationsUpdate } from "../../funcs/accountingLocationsUpdate.js";
import { AccountingLocationsUpdateRequest$zodSchema } from "../../models/accountinglocationsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingLocationsUpdateRequest$zodSchema,
};

export const tool$accountingLocationsUpdate: ToolDefinition<typeof args> = {
  name: "accounting-locations-update",
  description: `Update Location

Update Location`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingLocationsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
