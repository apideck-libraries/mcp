/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultConnectionSettingsList } from "../../funcs/vaultConnectionSettingsList.js";
import { VaultConnectionSettingsAllRequest$zodSchema } from "../../models/vaultconnectionsettingsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultConnectionSettingsAllRequest$zodSchema,
};

export const tool$vaultConnectionSettingsList: ToolDefinition<typeof args> = {
  name: "vault-connection-settings-list",
  description: `Get resource settings

This endpoint returns custom settings and their defaults required by connection for a given resource.
`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultConnectionSettingsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
