/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFoldersCreate } from "../../funcs/fileStorageFoldersCreate.js";
import { FileStorageFoldersAddRequest$zodSchema } from "../../models/filestoragefoldersaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFoldersAddRequest$zodSchema,
};

export const tool$fileStorageFoldersCreate: ToolDefinition<typeof args> = {
  name: "file-storage-folders-create",
  description: `Create Folder

Create Folder`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFoldersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
