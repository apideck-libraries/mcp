/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { ecommerceOrdersGet } from "../../funcs/ecommerceOrdersGet.js";
import { EcommerceOrdersOneRequest$zodSchema } from "../../models/ecommerceordersoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: EcommerceOrdersOneRequest$zodSchema,
};

export const tool$ecommerceOrdersGet: ToolDefinition<typeof args> = {
  name: "ecommerce-orders-get",
  description: `Get Order

Get Order`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await ecommerceOrdersGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
