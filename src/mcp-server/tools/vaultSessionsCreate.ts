/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultSessionsCreate } from "../../funcs/vaultSessionsCreate.js";
import { VaultSessionsCreateRequest$zodSchema } from "../../models/vaultsessionscreateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultSessionsCreateRequest$zodSchema.optional(),
};

export const tool$vaultSessionsCreate: ToolDefinition<typeof args> = {
  name: "vault-sessions-create",
  description: `Create Session

Making a POST request to this endpoint will initiate a Hosted Vault session. Redirect the consumer to the returned
URL to allow temporary access to manage their integrations and settings.

Note: This is a short lived token that will expire after 1 hour (TTL: 3600).
`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultSessionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
