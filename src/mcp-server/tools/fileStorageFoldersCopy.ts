/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFoldersCopy } from "../../funcs/fileStorageFoldersCopy.js";
import { FileStorageFoldersCopyRequest$zodSchema } from "../../models/filestoragefolderscopyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFoldersCopyRequest$zodSchema,
};

export const tool$fileStorageFoldersCopy: ToolDefinition<typeof args> = {
  name: "file-storage-folders-copy",
  description: `Copy Folder

Copy Folder`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFoldersCopy(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
