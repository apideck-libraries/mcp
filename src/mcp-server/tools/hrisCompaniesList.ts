/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { hrisCompaniesList } from "../../funcs/hrisCompaniesList.js";
import { HrisCompaniesAllRequest$zodSchema } from "../../models/hriscompaniesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HrisCompaniesAllRequest$zodSchema.optional(),
};

export const tool$hrisCompaniesList: ToolDefinition<typeof args> = {
  name: "hris-companies-list",
  description: `List Companies

List Companies`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await hrisCompaniesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
