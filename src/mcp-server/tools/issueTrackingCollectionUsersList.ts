/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { issueTrackingCollectionUsersList } from "../../funcs/issueTrackingCollectionUsersList.js";
import { IssueTrackingCollectionUsersAllRequest$zodSchema } from "../../models/issuetrackingcollectionusersallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: IssueTrackingCollectionUsersAllRequest$zodSchema,
};

export const tool$issueTrackingCollectionUsersList: ToolDefinition<
  typeof args
> = {
  name: "issue-tracking-collection-users-list",
  description: `List Users

List Users`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await issueTrackingCollectionUsersList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
