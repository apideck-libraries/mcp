/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmOpportunitiesDelete } from "../../funcs/crmOpportunitiesDelete.js";
import { CrmOpportunitiesDeleteRequest$zodSchema } from "../../models/crmopportunitiesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmOpportunitiesDeleteRequest$zodSchema,
};

export const tool$crmOpportunitiesDelete: ToolDefinition<typeof args> = {
  name: "crm-opportunities-delete",
  description: `Delete opportunity

Delete opportunity`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmOpportunitiesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
