/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { atsApplicationsGet } from "../../funcs/atsApplicationsGet.js";
import { AtsApplicationsOneRequest$zodSchema } from "../../models/atsapplicationsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AtsApplicationsOneRequest$zodSchema,
};

export const tool$atsApplicationsGet: ToolDefinition<typeof args> = {
  name: "ats-applications-get",
  description: `Get Application

Get Application`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await atsApplicationsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
