/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFilesExport } from "../../funcs/fileStorageFilesExport.js";
import { FileStorageFilesExportRequest$zodSchema } from "../../models/filestoragefilesexportop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFilesExportRequest$zodSchema,
};

export const tool$fileStorageFilesExport: ToolDefinition<typeof args> = {
  name: "file-storage-files-export",
  description: `Export File

Export File`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFilesExport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
