/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fileStorageFilesSearch } from "../../funcs/fileStorageFilesSearch.js";
import { FileStorageFilesSearchRequest$zodSchema } from "../../models/filestoragefilessearchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: FileStorageFilesSearchRequest$zodSchema,
};

export const tool$fileStorageFilesSearch: ToolDefinition<typeof args> = {
  name: "file-storage-files-search",
  description: `Search Files

Search Files`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await fileStorageFilesSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
