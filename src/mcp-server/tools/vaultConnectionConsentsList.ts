/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vaultConnectionConsentsList } from "../../funcs/vaultConnectionConsentsList.js";
import { VaultConnectionConsentsAllRequest$zodSchema } from "../../models/vaultconnectionconsentsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: VaultConnectionConsentsAllRequest$zodSchema,
};

export const tool$vaultConnectionConsentsList: ToolDefinition<typeof args> = {
  name: "vault-connection-consents-list",
  description: `Get consent records

Get all consent records for a connection`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await vaultConnectionConsentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
