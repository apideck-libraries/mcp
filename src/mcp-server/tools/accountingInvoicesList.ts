/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoicesList } from "../../funcs/accountingInvoicesList.js";
import { AccountingInvoicesAllRequest$zodSchema } from "../../models/accountinginvoicesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoicesAllRequest$zodSchema.optional(),
};

export const tool$accountingInvoicesList: ToolDefinition<typeof args> = {
  name: "accounting-invoices-list",
  description: `List Invoices

List Invoices`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoicesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
