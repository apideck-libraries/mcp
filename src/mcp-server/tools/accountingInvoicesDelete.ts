/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoicesDelete } from "../../funcs/accountingInvoicesDelete.js";
import { AccountingInvoicesDeleteRequest$zodSchema } from "../../models/accountinginvoicesdeleteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoicesDeleteRequest$zodSchema,
};

export const tool$accountingInvoicesDelete: ToolDefinition<typeof args> = {
  name: "accounting-invoices-delete",
  description: `Delete Invoice

Delete Invoice`,
  scopes: ["destructive"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoicesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
