/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectorConnectorDocsGet } from "../../funcs/connectorConnectorDocsGet.js";
import { ConnectorConnectorDocsOneRequest$zodSchema } from "../../models/connectorconnectordocsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ConnectorConnectorDocsOneRequest$zodSchema,
};

export const tool$connectorConnectorDocsGet: ToolDefinition<typeof args> = {
  name: "connector-connector-docs-get",
  description: `Get Connector Doc content

Get Connector Doc content`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectorConnectorDocsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
