/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingJournalEntriesGet } from "../../funcs/accountingJournalEntriesGet.js";
import { AccountingJournalEntriesOneRequest$zodSchema } from "../../models/accountingjournalentriesoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingJournalEntriesOneRequest$zodSchema,
};

export const tool$accountingJournalEntriesGet: ToolDefinition<typeof args> = {
  name: "accounting-journal-entries-get",
  description: `Get Journal Entry

Get Journal Entry`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingJournalEntriesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
