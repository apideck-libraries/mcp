/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingExpenseReportsUpdate } from "../../funcs/accountingExpenseReportsUpdate.js";
import { AccountingExpenseReportsUpdateRequest$zodSchema } from "../../models/accountingexpensereportsupdateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingExpenseReportsUpdateRequest$zodSchema,
};

export const tool$accountingExpenseReportsUpdate: ToolDefinition<typeof args> =
  {
    name: "accounting-expense-reports-update",
    description: `Update Expense Report

Update Expense Report`,
    scopes: ["write"],
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await accountingExpenseReportsUpdate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
