/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmUsersCreate } from "../../funcs/crmUsersCreate.js";
import { CrmUsersAddRequest$zodSchema } from "../../models/crmusersaddop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmUsersAddRequest$zodSchema,
};

export const tool$crmUsersCreate: ToolDefinition<typeof args> = {
  name: "crm-users-create",
  description: `Create user

Create user`,
  scopes: ["write"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmUsersCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
