/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingInvoiceItemsGet } from "../../funcs/accountingInvoiceItemsGet.js";
import { AccountingInvoiceItemsOneRequest$zodSchema } from "../../models/accountinginvoiceitemsoneop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingInvoiceItemsOneRequest$zodSchema,
};

export const tool$accountingInvoiceItemsGet: ToolDefinition<typeof args> = {
  name: "accounting-invoice-items-get",
  description: `Get Invoice Item

Get Invoice Item`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingInvoiceItemsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
