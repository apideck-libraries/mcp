/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountingAttachmentsList } from "../../funcs/accountingAttachmentsList.js";
import { AccountingAttachmentsAllRequest$zodSchema } from "../../models/accountingattachmentsallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AccountingAttachmentsAllRequest$zodSchema,
};

export const tool$accountingAttachmentsList: ToolDefinition<typeof args> = {
  name: "accounting-attachments-list",
  description: `List Attachments

List Attachments`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await accountingAttachmentsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
