/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmActivitiesList } from "../../funcs/crmActivitiesList.js";
import { CrmActivitiesAllRequest$zodSchema } from "../../models/crmactivitiesallop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CrmActivitiesAllRequest$zodSchema.optional(),
};

export const tool$crmActivitiesList: ToolDefinition<typeof args> = {
  name: "crm-activities-list",
  description: `List activities

List activities`,
  scopes: ["read"],
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await crmActivitiesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
